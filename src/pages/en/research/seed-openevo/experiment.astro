---
import AppLayout from '../../../../layouts/AppLayout.astro';
import OpenEvoExperimentGateway from '../../../../components/research/OpenEvoExperimentGateway.astro';
const locale = 'en' as const;
---
<AppLayout
  title="OpenEvo × WebShop / ALFWorld experiment entry"
  description="A dialogue-first explanation of WebShop and ALFWorld observations, model actions, environment feedback, evaluation signals, and the evidence boundary for OpenEvo evolution artifacts."
  locale={locale}
>
  <OpenEvoExperimentGateway locale={locale} />
</AppLayout>
