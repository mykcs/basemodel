---
import AppLayout from '../../../../layouts/AppLayout.astro';
import SeedOpenEvoComparisonDiagram from '../../../../components/research/SeedOpenEvoComparisonDiagram.astro';
import SeedOpenEvoResearchPage from '../../../../components/research/SeedOpenEvoResearchPage.astro';
const locale = 'en' as const;
---
<AppLayout title="SEED and OpenEvo loop comparison" description="Compare SEED's parameter-update loop with OpenEvo's cross-task carrier/revision loop, including experience flow, updated objects, activation boundaries, and the current experimental position." locale={locale}>
  <SeedOpenEvoResearchPage locale={locale} page="loops" />
  <SeedOpenEvoComparisonDiagram locale={locale} />
</AppLayout>
