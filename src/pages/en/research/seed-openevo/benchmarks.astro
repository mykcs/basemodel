---
import AppLayout from '../../../../layouts/AppLayout.astro';
import SeedOpenEvoResearchPage from '../../../../components/research/SeedOpenEvoResearchPage.astro';
const locale = 'en' as const;
---
<AppLayout title="ALFWorld and WebShop experiment protocol" description="Explain both agent benchmarks, action spaces, success criteria, normalized task scores, macro averages, failure modes, and controlled comparison." locale={locale}>
  <SeedOpenEvoResearchPage locale={locale} page="benchmarks" />
</AppLayout>
