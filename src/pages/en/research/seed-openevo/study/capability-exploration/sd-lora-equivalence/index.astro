---
import AppLayout from '../../../../../../../layouts/AppLayout.astro';
import OpenEvoSdLoraV2Outcome from '../../../../../../../components/research/OpenEvoSdLoraV2Outcome.astro';
import ResearchRouteContext from '../../../../../../../components/research/ResearchRouteContext.astro';
const locale = 'en' as const;
---
<AppLayout
  title="SD-LoRA v2: what we did after strict equivalence failed"
  description="Strict-equivalence acceleration found no qualified successor. SD-LoRA v2 is about 2× faster in the matched eight-step trainer, with no aggregate reward/success degradation observed on the prospective non-final WebShop panel."
  locale={locale}
>
  <ResearchRouteContext locale={locale} route="sd-lora-equivalence" />
  <OpenEvoSdLoraV2Outcome locale={locale} />
</AppLayout>
