---
import AppLayout from '../../../../../../../layouts/AppLayout.astro';
import OpenEvoRedesignMap from '../../../../../../../components/research/OpenEvoRedesignMap.astro';
const locale = 'en' as const;
---
<AppLayout
  title="Redesigning OpenEvo"
  description="The OpenEvo successor restarts from Harness201.1 and a fresh Stage 1: a new action interface, MiniMax post-hoc analysis, a sealed teacher-evidence pool, and fresh OPSD / Memory / Skill / Agent state form the new Stage-2 origin. The old 3B lineage remains historical evidence rather than a successor prefix."
  locale={locale}
>
  <OpenEvoRedesignMap locale={locale} />
</AppLayout>
