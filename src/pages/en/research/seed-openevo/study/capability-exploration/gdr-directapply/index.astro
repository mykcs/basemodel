---
import AppLayout from '../../../../../../../layouts/AppLayout.astro';
import OpenEvoGdrDirectApplyExplainer from '../../../../../../../components/research/OpenEvoGdrDirectApplyExplainer.astro';
import ResearchRouteContext from '../../../../../../../components/research/ResearchRouteContext.astro';
const locale = 'en' as const;
---
<AppLayout
  title="GDR → DirectApply: why 44 learning attempts left only 7 updates"
  description="Stage 2 trained 44 SD-LoRA candidate updates and GDR-v1 admitted only 7. That explains the update count; it does not establish worse final performance or a DirectApply advantage. No-GDR removes short-horizon task-score admission while keeping engineering and data-safety contracts."
  locale={locale}
>
  <ResearchRouteContext locale={locale} route="gdr-directapply" compact linkedLocation={false} />
  <OpenEvoGdrDirectApplyExplainer locale={locale} />
</AppLayout>
