---
import AppLayout from '../../../../../../../layouts/AppLayout.astro';
import OpenEvoGatedDeltaSdLoraExplainer from '../../../../../../../components/research/OpenEvoGatedDeltaSdLoraExplainer.astro';
const locale = 'en' as const;
---
<AppLayout
  title="Gated-Delta SD-LoRA: real parameter updates run, efficacy comparison still incomplete"
  description="Old GDR-v1 filtered an update after training; current Gated Delta changes LoRA parameters during learning. A real WebShop parameter write has run, but the full four-round Vanilla-vs-GDR efficacy comparison is not complete yet."
  locale={locale}
>
  <OpenEvoGatedDeltaSdLoraExplainer locale={locale} />
</AppLayout>
