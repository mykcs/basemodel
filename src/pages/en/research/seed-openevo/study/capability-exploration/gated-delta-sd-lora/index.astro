---
import AppLayout from '../../../../../../../layouts/AppLayout.astro';
import OpenEvoGatedDeltaSdLoraExplainer from '../../../../../../../components/research/OpenEvoGatedDeltaSdLoraExplainer.astro';
const locale = 'en' as const;
---
<AppLayout
  title="Gated-Delta SD-LoRA: real execution proved, full paired D1 still incomplete"
  description="Why historical GDR-v1 candidate admission is not the Gated Delta Rule we now study; how recurrent LoRA-factor State is updated; and why Route S proves real WebShop execution while the full Vanilla-vs-GDR paired D1 remains incomplete."
  locale={locale}
>
  <OpenEvoGatedDeltaSdLoraExplainer locale={locale} />
</AppLayout>
