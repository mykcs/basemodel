---
import AppLayout from '../../../../../../../layouts/AppLayout.astro';
import OpenEvoStage1EvolutionMap from '../../../../../../../components/research/OpenEvoStage1EvolutionMap.astro';
const locale = 'en' as const;
---
<AppLayout
  title="How Stage 1 was repaired step by step"
  description="Why were 1,440 WebShop trajectories still not enough to call the old run the final Stage 1? This map follows the interface failure, paired 3B / 1.7B diagnostics, and the shared 202609030400 design that made a new corpus suitable for downstream analysis."
  locale={locale}
>
  <OpenEvoStage1EvolutionMap locale={locale} />
</AppLayout>
