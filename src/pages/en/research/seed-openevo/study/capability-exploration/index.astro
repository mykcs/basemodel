---
import AppLayout from '../../../../../../layouts/AppLayout.astro';
import OpenEvoCapabilityMapLobby from '../../../../../../components/research/OpenEvoCapabilityMapLobby.astro';
const locale = 'en' as const;
---
<AppLayout
  title="OpenEvo capability exploration"
  description="OpenEvo capability exploration is split into two maps: a first-run map preserving the old Stage 1, Stage 2, sustained 7B evolution, and 3B diagnostic evidence; and a redesign map that starts a successor from a new action interface and fresh Stage 1."
  locale={locale}
>
  <OpenEvoCapabilityMapLobby locale={locale} />
</AppLayout>
