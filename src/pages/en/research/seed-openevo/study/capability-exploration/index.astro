---
import AppLayout from '../../../../../../layouts/AppLayout.astro';
import OpenEvoCapabilityMapLobby from '../../../../../../components/research/OpenEvoCapabilityMapLobby.astro';
const locale = 'en' as const;
---
<AppLayout
  title="OpenEvo capability exploration"
  description="OpenEvo capability exploration keeps two primary maps for the historical first run and the current redesign, plus focused roguelike maps explaining how Stage 1 became one shared harness and what the 7B Stage-2 SD-LoRA, Task Vector, capacity-amendment, and transfer evidence actually means."
  locale={locale}
>
  <OpenEvoCapabilityMapLobby locale={locale} />
</AppLayout>
