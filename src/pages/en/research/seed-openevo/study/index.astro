---
import AppLayout from '../../../../../layouts/AppLayout.astro';
import SeedOpenEvoStudyOverview from '../../../../../components/research/SeedOpenEvoStudyOverview.astro';
const locale = 'en' as const;
---

<AppLayout
  title="OpenEVO (Harness) · WebShop dataset experiments"
  description="We test how OpenEVO learns on WebShop, whether learning actually improves task performance, and how that evidence compares with baselines and SEED. This page summarizes what we did, what we have observed so far, and what we still cannot conclude."
  locale={locale}
>
  <SeedOpenEvoStudyOverview locale={locale} />
</AppLayout>
