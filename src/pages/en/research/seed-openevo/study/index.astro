---
import AppLayout from '../../../../../layouts/AppLayout.astro';
import SeedOpenEvoStudyOverview from '../../../../../components/research/SeedOpenEvoStudyOverview.astro';
const locale = 'en' as const;
---

<AppLayout
  title="OpenEvo × SEED: WebShop study"
  description="Start from the scientific questions: why the OpenEvo × SEED WebShop experiments exist, how they connect, what the current evidence supports, and what remains unknown."
  locale={locale}
>
  <SeedOpenEvoStudyOverview locale={locale} />
</AppLayout>
