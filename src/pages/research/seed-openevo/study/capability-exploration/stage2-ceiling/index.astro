---
import AppLayout from '../../../../../../layouts/AppLayout.astro';
import OpenEvoCeilingStrategy from '../../../../../../components/research/OpenEvoCeilingStrategy.astro';
const locale = 'zh' as const;
---
<AppLayout title="第二版 Stage 2：旧门槛去掉了，但 3B 仍然没有学起来" description="Ceiling-1.0 去掉了旧的 7 对 8 门槛，改为每 128 次任务形成一轮证据。快照中 7B 已经产生 25 个 SD-LoRA 组件，而 3B 跑完 18 轮仍是 0；后续 Harness 诊断进一步发现 action contract 会改变模型实际产生的轨迹，因此新版 successor 从 fresh Stage 1 重新开始。" locale={locale}>
  <OpenEvoCeilingStrategy locale={locale} />
</AppLayout>
