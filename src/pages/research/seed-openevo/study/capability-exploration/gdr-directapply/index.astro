---
import AppLayout from '../../../../../../layouts/AppLayout.astro';
import OpenEvoGdrDirectApplyExplainer from '../../../../../../components/research/OpenEvoGdrDirectApplyExplainer.astro';
const locale = 'zh' as const;
---
<AppLayout
  title="历史 GDR-v1 / DirectApply：44 个候选更新为什么只留下 7 个"
  description="旧 GDR-v1 每轮会训练一个 SD-LoRA 候选更新，然后用固定 16 题决定是否保留；44 个候选里只有 7 个真正改到了后续模型。DirectApply 取消了这道筛选，但两次 final 题集不同，分数不能直接相减。"
  locale={locale}
>
  <OpenEvoGdrDirectApplyExplainer locale={locale} />
</AppLayout>
