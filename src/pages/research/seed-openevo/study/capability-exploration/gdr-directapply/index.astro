---
import AppLayout from '../../../../../../layouts/AppLayout.astro';
import OpenEvoGdrDirectApplyExplainer from '../../../../../../components/research/OpenEvoGdrDirectApplyExplainer.astro';
import ResearchRouteContext from '../../../../../../components/research/ResearchRouteContext.astro';
const locale = 'zh' as const;
---
<AppLayout
  title="GDR → DirectApply：44 次学习为什么只留下 7 次更新"
  description="1.7B Stage2 实际训练 44 个 SD-LoRA 候选更新，GDR-v1 只放行 7 个；这解释更新次数，但不证明 GDR 降低最终性能或 DirectApply 更好。No-GDR 只移除短期 task-score 放行规则，工程与数据安全合同继续保留。"
  locale={locale}
>
  <ResearchRouteContext locale={locale} route="gdr-directapply" compact linkedLocation={false} />
  <OpenEvoGdrDirectApplyExplainer locale={locale} />
</AppLayout>
