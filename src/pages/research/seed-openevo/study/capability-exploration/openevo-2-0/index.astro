---
import AppLayout from '../../../../../../layouts/AppLayout.astro';
import OpenEvoRedesignMap from '../../../../../../components/research/OpenEvoRedesignMap.astro';
const locale = 'zh' as const;
---
<AppLayout
  title="重新设计 OpenEvo"
  description="OpenEvo successor 从 Harness201.1 与 fresh Stage 1 重新开始：新的行动接口、MiniMax 任务后分析、teacher evidence pool、OPSD / Memory / Skill / Agent 共同组成新的 Stage 2 起点；旧 3B lineage 保留为历史证据而不作为新版前缀。"
  locale={locale}
>
  <OpenEvoRedesignMap locale={locale} />
</AppLayout>
