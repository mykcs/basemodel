---
import AppLayout from '../../../../../../layouts/AppLayout.astro';
import OpenEvo2Strategy from '../../../../../../components/research/OpenEvo2Strategy.astro';
const locale = 'zh' as const;
---
<AppLayout title="Harness 2.0：先修好模型和 WebShop 的动作接口，再重跑 Stage 2" description="Ceiling-1.0 中 3B 跑了 18 轮仍没有产生新参数组件，而且大量失败来自选择当前页面不存在的动作。Harness 2.0 不重做 Stage 1，而是减少并重新分工历史经验提示，并用两个固定 128 次的小实验先验证动作接口是否恢复正常。" locale={locale}>
  <OpenEvo2Strategy locale={locale} />
</AppLayout>
