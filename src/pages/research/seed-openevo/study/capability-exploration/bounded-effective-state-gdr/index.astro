---
import AppLayout from '../../../../../../layouts/AppLayout.astro';
import OpenEvoEffectiveStateGdrLoraStudy from '../../../../../../components/research/OpenEvoEffectiveStateGdrLoraStudy.astro';
const locale = 'zh' as const;
---
<AppLayout
  title="三个 OpenEVO 1.7B 已完成实验：普通、Bounded、β-gating（α=1）"
  description="Qwen3-1.7B 的三组已完成 OpenEVO 实验：普通 OpenEVO、Bounded Online Recurrence、Bounded Online Recurrence + β-gating（α=1）。动态 α + 动态 β 尚未实验，页面只保留空位，不填结果。"
  locale={locale}
>
  <OpenEvoEffectiveStateGdrLoraStudy locale={locale} />
</AppLayout>
