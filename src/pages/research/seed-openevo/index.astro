---
import AppLayout from '../../../layouts/AppLayout.astro';
import SeedOpenEvoResearchPage from '../../../components/research/SeedOpenEvoResearchPage.astro';
const locale = 'zh' as const;
---
<AppLayout title="用 OpenEvo 复现 SEED 的 ALFWorld 与 WebShop 实验" description="按 SEED 的设置运行 ALFWorld 与 WebShop，再用相同模型、提示词、随机种子和预算运行 OpenEvo，保存分数、轨迹、失败原因与更新结果。" locale={locale}>
  <SeedOpenEvoResearchPage locale={locale} page="hub" />
</AppLayout>
