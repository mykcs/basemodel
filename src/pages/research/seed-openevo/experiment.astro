---
import AppLayout from '../../../layouts/AppLayout.astro';
import OpenEvoExperimentGateway from '../../../components/research/OpenEvoExperimentGateway.astro';
const locale = 'zh' as const;
---
<AppLayout title="OpenEvo × WebShop 实验" description="openevo-experiment 是科学 source of truth；本站保留带日期的默认分支快照与历史结果，实时阶段和 GPU 分配从实际工作 branch 的 current-campaign、最新 reconciliation/result 与 preregistration 解析。" locale={locale}>
  <OpenEvoExperimentGateway locale={locale} />
</AppLayout>