---
import AppLayout from '../layouts/AppLayout.astro';

interface Props { locale: 'zh' | 'en'; }
const { locale } = Astro.props;
const zh = locale === 'zh';
const t = (cn: string, en: string) => zh ? cn : en;
---

<AppLayout
  title={t('BaseModel 的开发与上线', 'How BaseModel is developed and shipped')}
  description={t(
    'BaseModel 当前用 GitHub 管理源码，用公开 GitHub Actions 做合并前 CI，用 Vercel 发布 Production 和按需 Preview，并由 Cloudflare 独立检查线上状态。',
    'BaseModel keeps source in GitHub, uses public GitHub Actions for pre-merge CI, ships Production and on-demand Previews with Vercel, and uses Cloudflare for independent live-site checks.',
  )}
  locale={locale}
>
  <main class="shell development-page">
    <header class="development-hero">
      <h1>{t('BaseModel 的开发与上线', 'How BaseModel is developed and shipped')}</h1>
      <p class="lede">
        {t(
          '我们在 ChatGPT 里提出和审阅改动，GitHub 保存代码并用免费的公开 Runner 做合并检查；通过后由 Vercel 发布 main，Cloudflare 再从外部检查线上状态。',
          'We start and review changes in ChatGPT. GitHub stores the code and uses free public runners for merge checks; after acceptance, Vercel ships main and Cloudflare checks the live site from the outside.',
        )}
      </p>
    </header>

    <section class="development-system" aria-labelledby="development-system-title">
      <h2 id="development-system-title">{t('一条主线', 'The main path')}</h2>
      <ol class="development-flow" aria-label={t('BaseModel 开发与上线流程', 'BaseModel development and release flow')}>
        <li data-role="control"><span>01</span><div><strong>ChatGPT</strong><small>{t('说清目标、审阅结果', 'set the goal, review the result')}</small></div></li>
        <li data-role="source"><span>02</span><div><strong>GitHub</strong><small>{t('源码、分支、PR、main', 'source, branches, PRs, main')}</small></div></li>
        <li data-role="accept"><span>03</span><div><strong>GitHub Actions</strong><small>{t('required CI · 4 路 Chromium', 'required CI · 4 Chromium shards')}</small></div></li>
        <li data-role="ship"><span>04</span><div><strong>Vercel Production</strong><small>{t('main 构建与上线', 'build and ship main')}</small></div></li>
        <li data-role="observe"><span>05</span><div><strong>Cloudflare</strong><small>{t('上线后健康检查', 'post-deploy health checks')}</small></div></li>
      </ol>
      <p class="system-boundary">
        {t(
          '合并前的浏览器 CI 已经放到 GitHub 的公开 Runner 上并行运行；Vercel 不再为了同一个候选重复跑一遍完整浏览器矩阵。',
          'Pre-merge browser CI runs in parallel on GitHub public runners; Vercel no longer repeats the full browser matrix for the same candidate.',
        )}
      </p>
    </section>

    <section aria-labelledby="tool-roles-title">
      <h2 id="tool-roles-title">{t('每个工具负责什么', 'What each tool does')}</h2>
      <div class="tool-roles">
        <article><h3>{t('ChatGPT 网页端', 'ChatGPT on the web')}</h3><p>{t('这是人的控制界面：提出目标、给反馈、看结果、决定是否继续。它不替代 GitHub 里的源码和历史。', 'This is the human control surface: set goals, give feedback, review results, and decide what happens next. It does not replace source or history in GitHub.')}</p></article>
        <article><h3>{t('GitHub 插件', 'GitHub plugin')}</h3><p>{t('让 Agent 直接读写仓库、PR、检查状态和评论。能走 API 的控制面优先走 API，不靠浏览器反复点按钮。', 'It lets the Agent read and write repositories, pull requests, checks, and comments directly. Provider control planes use APIs first instead of repeated browser clicking.')}</p></article>
        <article><h3>Remote Desktop Commander</h3><p>{t('需要真实文件系统、终端、Git、Node、Playwright 或本机 CLI 时，通过授权的电脑执行。它用于加快开发和本地验证，不是常驻 CI 服务器。', 'When the work needs the real filesystem, terminal, Git, Node, Playwright, or a local CLI, it runs on an authorized computer. It accelerates development and local validation; it is not an always-on CI server.')}</p></article>
        <article><h3>GitHub</h3><p>{t('源码的唯一主线。分支、PR、main、required check 和可审计历史都在这里；合并只认当前候选 SHA 和当前 main 上的检查结果。', 'The source-of-truth repository. Branches, PRs, main, required checks, and auditable history live here; merge acceptance belongs to the current candidate SHA and current main.')}</p></article>
        <article><h3>GitHub Actions</h3><p>{t('BaseModel 公开后，它成为主要 CI。每个 PR 用只读权限跑确定性检查和按风险选择的 Chromium；完整 UI 任务拆成 4 个独立 Runner 并行，最后由 public-ci-gate 汇总。', 'After BaseModel became public, GitHub Actions became the primary CI. Each PR runs read-only deterministic checks and risk-based Chromium coverage; full UI work is split across four independent runners and aggregated by public-ci-gate.')}</p></article>
        <article><h3>Vercel</h3><p>{t('负责正式网站 Production，也保留按需 Preview。Production 会做仓库验证和静态构建，但不重复 GitHub Actions 已经通过的完整浏览器 CI；需要看真实 Vercel Preview 时再显式请求。', 'Vercel owns the Production site and retains on-demand Previews. Production runs repository validation and the static build without repeating the full browser CI already passed in GitHub Actions; a real Vercel Preview is requested only when needed.')}</p></article>
        <article><h3>Cloudflare</h3><p>{t('不负责 BaseModel 的普通部署。一个很小的 Worker 定时从外部检查正式站点的 HTTP、canonical、robots、sitemap 和关键跳转。', 'Cloudflare is not the ordinary BaseModel deployment provider. A small Worker periodically checks the Production site from the outside: HTTP, canonical metadata, robots, sitemap, and key redirects.')}</p></article>
        <article><h3>CircleCI</h3><p>{t('2026-09-08，CircleCI credits 已耗尽，因此退出普通 PR/main 流程。已经验证过的配置仍保留，只在需要独立恢复或交叉验证时显式手动触发。', 'On 2026-09-08, the CircleCI credit balance was exhausted, so it left the ordinary PR/main path. The qualified configuration remains available only for explicit manual recovery or cross-checking.')}</p></article>
      </div>
    </section>

    <section class="ci-section" aria-labelledby="ci-title">
      <h2 id="ci-title">{t('CI 方案', 'CI design')}</h2>
      <p class="section-lede">
        {t(
          '现在的原则很简单：免费的 GitHub Runner 负责合并前的重测试，付费的 Vercel 只做它真正擅长的部署和必要 Preview。',
          'The rule is simple: free GitHub runners own heavy pre-merge testing; paid Vercel is reserved for deployment and Previews that are actually useful.',
        )}
      </p>
      <ol class="ci-steps">
        <li><strong>{t('PR push 自动检查。', 'Every PR push is checked automatically.')}</strong><span>{t('GitHub Actions 绑定 exact PR head 和当前 base；确定性检查与浏览器检查都在同一个候选身份上完成。', 'GitHub Actions binds the exact PR head and current base; deterministic and browser checks run against the same candidate identity.')}</span></li>
        <li><strong>{t('完整浏览器任务并行。', 'Full browser work runs in parallel.')}</strong><span>{t('共享、全局或无法判断的 UI 改动拆成 4 个独立 Chromium shard；局部改动仍按风险只跑必要覆盖。', 'Shared, global, or uncertain UI changes split into four independent Chromium shards; bounded changes still run only the mapped coverage required by risk.')}</span></li>
        <li><strong>{t('public-ci-gate 决定能不能合并。', 'public-ci-gate decides merge readiness.')}</strong><span>{t('GitHub main 的 required check 绑定 GitHub Actions。旧的 Vercel、CircleCI green 或别的 SHA 都不能借来当当前证据。', 'The required check on main is bound to GitHub Actions. An old Vercel or CircleCI green, or a result from another SHA, cannot be borrowed as current evidence.')}</span></li>
        <li><strong>{t('Preview 变成按需工具。', 'Preview becomes an on-demand tool.')}</strong><span>{t('只有真人需要看真实 Vercel 页面、排查 provider 差异或做恢复验证时，才移动持久 Preview ref；它不再卡住每次合并。', 'The persistent Preview ref moves only when a human needs a real Vercel page, a provider-specific diagnosis, or recovery evidence; it no longer blocks every merge.')}</span></li>
        <li><strong>{t('合并后只做上线需要的工作。', 'After merge, do only what shipping needs.')}</strong><span>{t('Vercel 对 main 做验证、静态构建和 Production 发布，不重复完整 Chromium；Cloudflare 再从外部检查正式站点。', 'Vercel validates, builds, and publishes main without repeating the full Chromium matrix; Cloudflare then checks the Production site externally.')}</span></li>
      </ol>

      <details class="technical-detail">
        <summary>{t('按需 Vercel Preview 的 exact-head 细节', 'Exact-head details for an on-demand Vercel Preview')}</summary>
        <p>{t('只有确实需要 Vercel Preview 时，Agent 才运行', 'Only when a Vercel Preview is genuinely needed does the Agent run')} <code>node scripts/request-vercel-final-gate.mjs &lt;PR_NUMBER&gt;</code>{zh ? '。' : '.'}</p>
        <p>{t('这个 helper 先把', 'The helper first pins')} <code>ci/vercel-gate-base</code> {t('固定到当前 main，再把持久的', 'to live main, then moves persistent')} <code>ci/vercel-gate-final</code> {t('移动到 PR 的 exact head。这个 Preview 是 provider 验证或人工审阅工具，不是 required merge check。', 'to the exact PR head. This Preview is for provider validation or human review, not the required merge check.')}</p>
      </details>
    </section>

    <section aria-labelledby="change-flow-title">
      <h2 id="change-flow-title">{t('一次改动怎样上线', 'How one change gets shipped')}</h2>
      <ol class="change-flow">
        <li><span>1</span><div><strong>{t('在 ChatGPT 里说清楚要改什么', 'Describe the change in ChatGPT')}</strong><p>{t('人负责目标、反馈和最终判断。', 'The human owns the goal, feedback, and final judgment.')}</p></div></li>
        <li><span>2</span><div><strong>{t('Agent 在 GitHub / 本机完成实现', 'The Agent implements through GitHub / the local machine')}</strong><p>{t('仓库读写优先 GitHub 插件；文件、终端和本机工具需要时用 Remote Desktop Commander。', 'Repository operations prefer the GitHub plugin; filesystem, terminal, and local tools use Remote Desktop Commander when needed.')}</p></div></li>
        <li><span>3</span><div><strong>{t('先做便宜的本地检查', 'Run cheap local checks first')}</strong><p>{t('类型检查、单元测试和静态 build 能在本地解决的问题，不留给云端重复发现。', 'Type checks, unit tests, and the static build catch predictable problems locally instead of rediscovering them in hosted CI.')}</p></div></li>
        <li><span>4</span><div><strong>{t('GitHub Actions 自动跑 required CI', 'GitHub Actions runs the required CI')}</strong><p>{t('确定性检查和 4 路 Chromium 在公开 Runner 上并行；需要真实 Preview 时再单独请求 Vercel。', 'Deterministic checks and four Chromium shards run on public runners; request Vercel separately only when a real Preview is useful.')}</p></div></li>
        <li><span>5</span><div><strong>{t('public-ci-gate 通过后合并 main', 'Merge to main after public-ci-gate passes')}</strong><p>{t('如果 main 或候选变了，就重新验当前 SHA。', 'If main or the candidate changes, acceptance is repeated on the new current SHA.')}</p></div></li>
        <li><span>6</span><div><strong>{t('检查 Production', 'Verify Production')}</strong><p>{t('确认 Vercel 正式部署，再看 Cloudflare smoke 和真实公开页面。', 'Confirm the Vercel Production deployment, then check Cloudflare smoke and the real public site.')}</p></div></li>
      </ol>
    </section>

    <section class="docs-boundary" aria-labelledby="docs-boundary-title">
      <h2 id="docs-boundary-title">{t('人看网页，Agent 看文档', 'Humans use this page; Agents use the docs')}</h2>
      <p>{t('这张页面只负责让人快速理解当前方案。Agent 开始工作时应该先读仓库里的 ', 'This page is the human-readable map. An Agent starting work should read ')}<code>AGENTS.md</code>、<code>docs/agents/current/ci-provider-decision.md</code>、<code>hosting-architecture.md</code> {t('和可执行配置。', 'and the executable configuration.')}</p>
      <p>{t('供应商额度、required checks、runner 状态都会变化，所以这里不把某个月的价格或次数写成永久规则。页面和文档如果与 live provider state 冲突，以 live state 和仓库可执行配置为准。', 'Provider quotas, required checks, and runner state can change. This page therefore does not freeze a monthly price or quota as permanent policy. If the page or docs disagree with live provider state, live state and executable repository configuration win.')}</p>
    </section>
  </main>
</AppLayout>

<style>
  .development-page { padding-block: clamp(2.5rem, 6vw, 5rem); }
  .development-hero { display: grid; align-content: center; min-height: calc(100svh - 80px); max-width: 860px; margin-bottom: clamp(3rem, 7vw, 6rem); }
  .development-hero h1 { max-width: 14ch; margin: 0; font-size: clamp(2.35rem, 6vw, 5.5rem); line-height: .98; letter-spacing: -.045em; }
  .development-hero .lede { max-width: 760px; margin: 1.35rem 0 0; color: var(--text); font-size: clamp(1.08rem, 2vw, 1.35rem); line-height: 1.65; }
  section { margin-block: clamp(3.5rem, 8vw, 7rem); }
  section > h2 { max-width: 760px; margin-bottom: 1.5rem; font-size: clamp(1.65rem, 3.3vw, 2.5rem); letter-spacing: -.025em; }
  .development-system { margin-top: 0; }
  .development-flow { display: grid; grid-template-columns: repeat(5, minmax(0, 1fr)); gap: clamp(1rem, 2vw, 1.7rem); margin: 1.5rem 0; padding: 0; list-style: none; }
  .development-flow li { position: relative; min-width: 0; padding: 1rem; border: 1px solid var(--line); border-radius: var(--radius-panel); background: var(--surface); }
  .development-flow li:not(:last-child)::after { content: ''; position: absolute; top: 50%; left: calc(100% + 1px); width: clamp(1rem, 2vw, 1.7rem); height: 1px; background: var(--line-strong, var(--line)); }
  .development-flow li:not(:last-child)::before { content: ''; position: absolute; z-index: 1; top: calc(50% - 4px); left: calc(100% + clamp(1rem, 2vw, 1.7rem) - 7px); width: 7px; height: 7px; border-top: 1px solid var(--line-strong, var(--line)); border-right: 1px solid var(--line-strong, var(--line)); transform: rotate(45deg); }
  .development-flow li[data-role='control'] { border-top-color: color-mix(in srgb, var(--accent) 55%, var(--line)); }
  .development-flow li[data-role='source'] { border-top-color: color-mix(in srgb, #6d5bd0 58%, var(--line)); }
  .development-flow li[data-role='accept'], .development-flow li[data-role='ship'] { border-top-color: color-mix(in srgb, #2f8f5b 58%, var(--line)); }
  .development-flow li[data-role='observe'] { border-top-color: color-mix(in srgb, #2a72b8 58%, var(--line)); }
  .development-flow span { display: block; margin-bottom: 1rem; color: var(--accent); font: 750 .72rem/1 ui-monospace, SFMono-Regular, Menlo, monospace; }
  .development-flow strong, .development-flow small { display: block; }
  .development-flow strong { font-size: .98rem; }
  .development-flow small { margin-top: .35rem; color: var(--muted); line-height: 1.45; }
  .system-boundary, .section-lede { max-width: 760px; color: var(--muted); font-size: 1.02rem; line-height: 1.75; }
  .tool-roles { border-top: 1px solid var(--line); }
  .tool-roles article { display: grid; grid-template-columns: minmax(170px, .7fr) minmax(0, 2fr); gap: 2rem; padding: 1.35rem 0; border-bottom: 1px solid var(--line); }
  .tool-roles h3, .tool-roles p { margin: 0; }
  .tool-roles h3 { font-size: 1rem; }
  .tool-roles p { max-width: 760px; color: var(--muted); line-height: 1.7; }
  .ci-section { max-width: 980px; }
  .ci-steps { margin: 2rem 0; padding: 0; border-top: 1px solid var(--line); list-style: none; }
  .ci-steps li { display: grid; grid-template-columns: minmax(190px, .85fr) minmax(0, 1.8fr); gap: 2rem; padding: 1.2rem 0; border-bottom: 1px solid var(--line); }
  .ci-steps strong, .ci-steps span { display: block; }
  .ci-steps span { color: var(--muted); line-height: 1.7; }
  .technical-detail { margin-top: 2rem; padding: 1rem 0; border-block: 1px solid var(--line); }
  .technical-detail summary { cursor: pointer; font-weight: 700; }
  .technical-detail p { max-width: 780px; color: var(--muted); line-height: 1.7; }
  .change-flow { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 0 2rem; margin: 0; padding: 0; border-top: 1px solid var(--line); list-style: none; }
  .change-flow li { display: grid; grid-template-columns: 2rem minmax(0, 1fr); gap: .8rem; padding: 1.3rem 0; border-bottom: 1px solid var(--line); }
  .change-flow li:nth-child(odd) { padding-right: 1rem; }
  .change-flow > li > span { color: var(--accent); font: 750 .72rem/1.5 ui-monospace, SFMono-Regular, Menlo, monospace; }
  .change-flow strong { display: block; }
  .change-flow p { margin: .35rem 0 0; color: var(--muted); line-height: 1.6; }
  .docs-boundary { max-width: 820px; padding-top: 1.5rem; border-top: 2px solid var(--text); }
  .docs-boundary p { color: var(--muted); line-height: 1.75; }
  code { font-size: .9em; overflow-wrap: anywhere; }
  @media (max-width: 860px) {
    .development-flow { grid-template-columns: 1fr; gap: 1.5rem; }
    .development-flow li { padding: 1rem; }
    .development-flow li:not(:last-child)::after { top: 100%; left: 50%; width: 1px; height: 1.5rem; }
    .development-flow li:not(:last-child)::before { top: calc(100% + 1.5rem - 8px); left: calc(50% - 3px); transform: rotate(135deg); }
    .tool-roles article, .ci-steps li { grid-template-columns: 1fr; gap: .5rem; }
    .change-flow { grid-template-columns: 1fr; }
    .change-flow li:nth-child(odd) { padding-right: 0; }
  }
</style>
