---
import { openEvoProgramLinks } from '../../data/openEvoWebShopProgram';

export type OpenEvoWebShopResultNoteId =
  | 'why-it-kept-failing'
  | 'first-positive-transfer'
  | 'independent-replication'
  | 'second-generation'
  | 'measurement-boundary'
  | 'current-conclusion';

interface Props { note: OpenEvoWebShopResultNoteId }
const { note } = Astro.props;
const root = '/research/seed-openevo/results';

const sealedRoot = 'https://github.com/mykcs/openevo-experiment/blob/0b6e7dcc718a85ed967feb85b5c95fd68f92d38e';
const h142Root = 'https://github.com/mykcs/openevo-experiment/blob/d1f35ecdf84c61b07df7c646e83588d63b9297bd';
const evidence = {
  h138b: `${sealedRoot}/docs/evidence/remote-runs/2026-08-20/h1.38b-correction-02/REPORT.md`,
  h139: `${sealedRoot}/docs/evidence/remote-runs/2026-08-20/h1.39-mr/REPORT.md`,
  h140: openEvoProgramLinks.reconciliation,
  h141: openEvoProgramLinks.h141Reconciliation,
  ledger: openEvoProgramLinks.ledger,
  h142: `${h142Root}/docs/evidence/remote-runs/2026-08-21/h1.42-mv/H142_STAGE_B_CLOSEOUT.md`,
  h142Reconciliation: `${h142Root}/docs/evidence/remote-runs/2026-08-21/h1.42-mv/stage-b-reconciliation.json`,
  h142Resources: `${h142Root}/docs/evidence/remote-runs/2026-08-21/h1.42-mv/stage-b-resource-accounting.json`,
} as const;

const notes = [
  {
    id: 'why-it-kept-failing',
    index: '04',
    title: '为什么我们前面一直没有得到可靠的 fresh-task 提升？',
    description: '从 3B、7B、parser / fallback 到冻结配对面板：先把“工程上跑通”和“科学上能下结论”分开。',
  },
  {
    id: 'first-positive-transfer',
    index: '05',
    title: '第一次正结果：什么时候第一次看到可迁移收益？',
    description: 'H1.38B continuation 给出第一条置信区间完全位于 0 以上的 fresh-task 结果。',
  },
  {
    id: 'independent-replication',
    index: '06',
    title: '独立复现：换一批全新 WebShop 任务以后，提升还在吗？',
    description: '冻结训练产物、换独立新任务面板后，同方向提升再次出现，这是当前最强的单代迁移证据。',
  },
  {
    id: 'second-generation',
    index: '07',
    title: '第二次连续写入：为什么还不能说“多代自进化”成立？',
    description: '第二轮成功经验并不缺，G2 也真实写进了参数；问题出在新旧能力无法同时稳定保留。',
  },
  {
    id: 'measurement-boundary',
    index: '08',
    title: 'H1.42：为什么修完测量器以后，机制比较仍然不能下结论？',
    description: 'Stage A 通过 calibration，但 Stage B 修正 validator 语义后仍有两个无效 cluster；结果停在 measurement boundary。',
  },
  {
    id: 'current-conclusion',
    index: '09',
    title: '现在到底能下什么结论？什么还不能说？',
    description: '把已经支持、明确失败、测量无效和仍未知四类状态分开，避免把研究叙事写得比证据更强。',
  },
] as const;

const current = notes.find((item) => item.id === note)!;
---

<article class="result-note">
  <header class="note-hero">
    <div class="note-topline">
      <a href={`${root}/`}>← 返回实验结果目录</a>
      <span>OPENEVO × WEBSHOP · 实验笔记 {current.index}</span>
    </div>
    <h1>{current.title}</h1>
    <p class="dek">{current.description}</p>
  </header>

  <nav class="series-nav" aria-label="OpenEvo WebShop 实验笔记">
    {notes.map((item) => (
      <a class:list={[{ active: item.id === note }]} href={`${root}/${item.id}/`}>
        <span>{item.index}</span>{item.title}
      </a>
    ))}
  </nav>

  {note === 'why-it-kept-failing' && (
    <main class="note-body">
      <section>
        <h2>1. 最早的问题不是“有没有更新参数”，而是模型根本做不成任务</h2>
        <p>早期 3B 路线里，参数写入、adapter 保存和重新加载都能工作，但 WebShop 闭环表现仍然接近 0。后来扩大到 7B，能力门有所改善，却仍然没有稳定的 fresh-task paired gain。</p>
        <p>这个阶段最重要的收获不是正结果，而是确认：<strong>参数发生变化、训练 loss 下降、甚至某些行为 probe 改变，都不能替代真正的闭环任务收益。</strong></p>
      </section>

      <section>
        <h2>2. parser、fallback、task identity 和时期漂移都必须先排除</h2>
        <p>WebShop 的一个动作格式错误，就可能让环境执行 fallback。只要 parser-invalid attempt 混进统计分母，就很难知道比较的是模型能力还是执行器容错。因此后续实验逐步把动作解析、fallback、任务身份、运行代码 SHA 和 matched evaluator 都写进冻结契约。</p>
        <p>H1.37-R 把 BASE 在同期重新跑了一次，BASE-original 与 BASE-R 保持一致，排除了“只是运行时期漂移”这一简单解释。之后 H1.38A-C 用完整冻结面板收口：四臂共 768 attempts、764 次科学有效，但 D4 / D8 / D16 相对 BASE 的 task-cluster 95% CI 都跨 0，而且 ordered delta 不单调。</p>
      </section>

      <aside class="takeaway">
        <strong>这个阶段真正建立的东西</strong>
        <p>不是“OpenEvo 无效”，而是一套更严格的证据规则：fresh task、matched arm、完整 denominator、parser validity、冻结代码和 cluster-level inference 必须同时满足。</p>
      </aside>

      <p class="evidence-line"><a href={evidence.ledger}>查看程序级实验 ledger →</a></p>
    </main>
  )}

  {note === 'first-positive-transfer' && (
    <main class="note-body">
      <section>
        <h2>1. 第一次正结果出现在 H1.38B continuation</h2>
        <p>这次实验完成 184 / 184 cells、704 / 704 attempts，其中 668 次科学有效，36 次因为 parser / fallback 被判 invalid。selector 选择 B1：SD-LoRA 相对同一批 fresh-task BASE 的平均 delta 为 <strong>+0.124</strong>，95% CI 为 <strong>[0.0326, 0.2194]</strong>。</p>
        <div class="metric-grid">
          <div><span>704</span><small>total attempts</small></div>
          <div><span>668</span><small>scientifically valid</small></div>
          <div><span>+0.124</span><small>SD − BASE</small></div>
          <div><span>[0.0326, 0.2194]</span><small>95% CI</small></div>
        </div>
      </section>

      <section>
        <h2>2. 为什么它比前面的“看起来变好了”更重要？</h2>
        <p>训练记录和 evaluation task identity 被分开，比较发生在训练未见任务上；同时 invalid attempts 没有被悄悄算进有效分母。它仍然只是一次 discovery，不足以单独支撑强泛化结论，但第一次把问题从“是否可能有迁移”推进到了“能否在独立新任务上再次复现”。</p>
      </section>

      <aside class="takeaway">
        <strong>最谨慎的表述</strong>
        <p>第一次参数写入出现了正向 fresh-task transfer signal；下一步必须冻结产物并更换独立任务面板复现。</p>
      </aside>

      <p class="evidence-line"><a href={evidence.h138b}>查看 H1.38B sealed report →</a></p>
    </main>
  )}

  {note === 'independent-replication' && (
    <main class="note-body">
      <section>
        <h2>1. 复现不是继续在原面板上调，而是冻结产物以后换一批任务</h2>
        <p>H1.39-MR 的核心价值在设计上：训练产物冻结，不再根据 evaluation 结果调参，然后换成独立的新 WebShop task identities。整轮共有 768 attempts、740 次科学有效，最终形成 58 个 complete clusters。</p>
      </section>

      <section>
        <h2>2. 两条更新路线都高于同一个 BASE</h2>
        <div class="metric-table" role="table" aria-label="H1.39-MR replication results">
          <div class="row head" role="row"><span>比较</span><span>delta</span><span>95% CI</span></div>
          <div class="row" role="row"><span>ordinary − BASE</span><strong>+0.163724</strong><span>[0.078086, 0.251931]</span></div>
          <div class="row" role="row"><span>SD − BASE</span><strong>+0.248845</strong><span>[0.164155, 0.336517]</span></div>
          <div class="row" role="row"><span>SD − ordinary</span><strong>+0.085121</strong><span>[0.002845, 0.167966]</span></div>
        </div>
        <p>三臂均值分别约为 BASE 0.118790、ordinary 0.290533、SD 0.384286。最重要的不是某一个最高分，而是：换成独立 fresh-task panel 后，更新后的模型仍然相对同一个 BASE 出现同方向提升。</p>
      </section>

      <aside class="takeaway">
        <strong>当前最强证据</strong>
        <p>模型自身成功经验经过一次参数更新后，可以在训练未见任务上产生可复现收益。它支持 single-generation transfer，但还没有回答连续写第二次、第三次是否仍然成立。</p>
      </aside>

      <p class="evidence-line"><a href={evidence.h139}>查看 H1.39-MR sealed report →</a></p>
    </main>
  )}

  {note === 'second-generation' && (
    <main class="note-body">
      <section>
        <h2>1. 第二轮并不缺成功经验</h2>
        <p>H1.40 的 E2 harvest 共 256 attempts，得到 132 个 qualified positives、覆盖 33 个 qualifying task identities；随后选出 16 条记录并真实创建 G2，包含 16 个新增 components、effective rank 64。第二轮失败不能简单归因于“模型没有产生可学经验”或“更新器根本没写进去”。</p>
        <div class="metric-grid">
          <div><span>256</span><small>E2 harvest attempts</small></div>
          <div><span>132</span><small>qualified positives</small></div>
          <div><span>33</span><small>qualifying identities</small></div>
          <div><span>16</span><small>G2 components</small></div>
        </div>
      </section>

      <section>
        <h2>2. 真正卡住的是 stability–plasticity</h2>
        <p>G2 写完之后，上游门要求同时回答三件事：新经验有没有学进去（acquisition）、旧能力有没有保留（retention）、H1.39 已经复现过的 fresh-task 收益有没有保住（preservation）。这三道门没有一起通过，因此预留的 T2 fresh-transfer panel 没有打开。</p>
        <p>随后 H1.41 用 C0 exact updater 与 C1 reset-magnitude 做更窄的机制筛选，完成 96 cells / 384 attempts。C0 acquisition point estimate 为 0，C0 / C1 retention 都为 −0.065625；C1 在 acquisition 与 preservation 各出现一个 invalid cluster，最终 selector 为 <strong>MEASUREMENT_INVALID</strong>。</p>
      </section>

      <aside class="takeaway">
        <strong>所以“第二代失败”不是一句话能解释的</strong>
        <p>经验供给不是当前直接瓶颈，但连续参数写入还没有同时解决学习新东西与保留旧能力的问题；H1.41 又提醒我们，机制比较本身必须先过测量有效性门。</p>
      </aside>

      <p class="evidence-line"><a href={evidence.h140}>查看 H1.40 reconciliation →</a> · <a href={evidence.h141}>查看 H1.41 reconciliation →</a></p>
    </main>
  )}

  {note === 'measurement-boundary' && (
    <main class="note-body">
      <section>
        <h2>1. H1.42 先问的不是“哪个 updater 更好”，而是“这个比较能不能被可靠测出来”</h2>
        <p>H1.41 已经暴露出 invalid measurement clusters，所以 H1.42-MV 把问题收窄为 measurement validity。Stage A 完成 <strong>192 cells / 768 attempts</strong>，通过冻结 calibration，selector 为 <strong>MV4 MEASUREMENT_VALIDATED</strong>；只有通过这道门，条件 Stage B 才继续运行。</p>
        <div class="metric-grid">
          <div><span>192</span><small>Stage A cells</small></div>
          <div><span>768</span><small>Stage A attempts</small></div>
          <div><span>4.162 h</span><small>Stage A GPU-hours</small></div>
          <div><span>MV4</span><small>measurement validated</small></div>
        </div>
      </section>

      <section>
        <h2>2. Stage B 跑完了，但修正 validator 以后仍然不能解释 mechanism contrast</h2>
        <p>Stage B 完成 <strong>144 / 144 cells、576 / 576 formal attempts</strong>。第一次 reconciliation 曾把 scientific treatment 字段 <code>arm</code> 错拿去和 loader identity <code>sd-lora</code> 比较，导致所有行被误判为 runner-identity；修正后使用同一批 immutable receipts 重新 reconciliation。</p>
        <p>修正没有把结果“变成正结果”。C1 reset-magnitude 在 acquisition 的 <code>webshop_003560</code> 和 preservation 的 <code>webshop_004357</code> 仍各有一个 parser/runtime-invalid cluster；retention 三个 arms 的八个 task clusters 则全部有效。冻结 selector 因此选择 <strong>MVD0 REMEASUREMENT_INVALID</strong>。</p>
        <div class="metric-grid">
          <div><span>144</span><small>Stage B cells</small></div>
          <div><span>576</span><small>Stage B attempts</small></div>
          <div><span>2.336 h</span><small>Stage B GPU-hours</small></div>
          <div><span>MVD0</span><small>remeasurement invalid</small></div>
        </div>
      </section>

      <section>
        <h2>3. 这篇文章的结论是“停在测量边界”，不是“magnitude reset 无效”</h2>
        <p>完整有效的 treatment panel 是解释机制差异的前提。既然 Stage B 仍有无效 clusters，H1.42 就不能支持 magnitude-reset mechanism effect，也不能支持 fresh-task transfer、multi-generation self-evolution 或任何 T2 结论。H1.40、H1.41、H1.42 的 T2 都保持关闭。</p>
        <p>H1.42 总计可核算 evaluation compute 为 <strong>6.4979 GPU-hours</strong>，unknown resource intervals 为 0。这个数字说明实验确实完整执行并可审计，但“完整跑完”仍然不等于“科学比较有效”。</p>
      </section>

      <aside class="takeaway">
        <strong>这里最重要的研究纪律</strong>
        <p>修复测量代码以后必须重新按冻结规则判断 validity，而不是因为已经花了 compute 就强行解释 arm difference。</p>
      </aside>

      <p class="evidence-line"><a href={evidence.h142}>查看 H1.42 closeout →</a> · <a href={evidence.h142Reconciliation}>reconciliation →</a> · <a href={evidence.h142Resources}>resource accounting →</a></p>
    </main>
  )}

  {note === 'current-conclusion' && (
    <main class="note-body">
      <section>
        <h2>1. 已经可以说：一次参数写入的跨任务收益可以复现</h2>
        <p>H1.38B discovery 与 H1.39-MR independent replication 给出同方向证据。最稳妥的能力结论是：<strong>模型自身成功经验经过一次参数更新后，可以在训练未见 WebShop tasks 上带来可复现收益。</strong></p>
      </section>

      <section>
        <h2>2. 还不能说：我们已经建立了多代 self-evolution</h2>
        <p>第二轮虽然 harvest 成功、G2 也真实生成，但 acquisition、retention、preservation 没有同时过门。H1.41 机制筛选出现 invalid clusters；H1.42 专门修复 measurement contract 后，Stage B 仍停在 <strong>MVD0 REMEASUREMENT_INVALID</strong>。因此“第二代没有成功”和“已经知道失败的唯一因果机制”仍然是两件不同的事。</p>
      </section>

      <section>
        <h2>3. 当前证据应该分成四类</h2>
        <div class="claim-grid">
          <div><small>SUPPORTED</small><h3>单代 fresh-task transfer</h3><p>第一次正结果被独立新任务复现。</p></div>
          <div><small>NEGATIVE</small><h3>第二代 upstream gates</h3><p>连续写入未同时保住 acquisition、retention 与 preservation。</p></div>
          <div><small>INVALID</small><h3>H1.41 / H1.42 mechanism measurement</h3><p>修正测量器后仍有 invalid clusters，不能把 arm difference 解释成可靠机制效应。</p></div>
          <div><small>UNKNOWN</small><h3>怎样稳定连续写入</h3><p>magnitude、replay、component interference 等仍是待验证假设，而不是定论。</p></div>
        </div>
      </section>

      <aside class="takeaway">
        <strong>研究主问题已经变了</strong>
        <p>现在不再是“参数写入到底有没有可能产生 transfer”，而是“怎样让第二次连续写入既获得新能力，又不破坏已有能力，并且能够被可靠测量”。</p>
      </aside>

      <p class="evidence-line"><a href={evidence.ledger}>查看完整 program ledger →</a> · <a href={evidence.h142}>查看最新 H1.42 closeout →</a></p>
    </main>
  )}
</article>

<style>
  .result-note{max-width:980px;margin:0 auto;padding:32px 24px 80px;color:var(--color-text)}
  .note-hero{padding:18px 0 42px;border-bottom:1px solid var(--color-border)}
  .note-topline{display:flex;justify-content:space-between;gap:20px;margin-bottom:36px;color:var(--color-text-muted);font-size:.76rem;font-weight:700;letter-spacing:.08em}.note-topline a{color:var(--color-accent);text-decoration:none}.note-topline a:hover{text-decoration:underline}
  .note-hero h1{margin:0;max-width:880px;font-size:clamp(2.15rem,5vw,4.35rem);line-height:1.04;letter-spacing:-.05em}.dek{max-width:760px;margin:22px 0 0;color:var(--color-text-muted);font-size:1.05rem;line-height:1.75}
  .series-nav{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;margin:20px 0 52px}.series-nav a{display:flex;gap:8px;align-items:flex-start;padding:12px;border:1px solid var(--color-border);border-radius:10px;color:var(--color-text-muted);font-size:.73rem;line-height:1.35;text-decoration:none}.series-nav a span{font:600 .7rem ui-monospace,SFMono-Regular,Menlo,monospace}.series-nav a.active{background:var(--color-surface);border-color:var(--color-accent);color:var(--color-text)}
  .note-body{max-width:800px;margin:0 auto}.note-body section{margin:0 0 46px}.note-body h2{margin:0 0 16px;font-size:clamp(1.45rem,3vw,2.15rem);line-height:1.2;letter-spacing:-.03em}.note-body p{margin:0 0 16px;color:var(--color-text-muted);font-size:1rem;line-height:1.82}.note-body strong{color:var(--color-text)}.note-body code{font-size:.9em}
  .metric-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px;margin:24px 0}.metric-grid div{padding:18px;border:1px solid var(--color-border);border-radius:12px;background:var(--color-surface)}.metric-grid span{display:block;color:var(--color-text);font:700 1.28rem ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:-.03em}.metric-grid small{display:block;margin-top:8px;color:var(--color-text-muted);font-size:.72rem;line-height:1.35}
  .metric-table{margin:22px 0;border:1px solid var(--color-border);border-radius:12px;overflow:hidden}.metric-table .row{display:grid;grid-template-columns:1.2fr .75fr 1fr;gap:12px;padding:12px 14px;border-top:1px solid var(--color-border);font-size:.85rem}.metric-table .row:first-child{border-top:0}.metric-table .head{background:var(--color-surface);color:var(--color-text-muted);font-size:.72rem;font-weight:700;letter-spacing:.04em}.metric-table strong{font-family:ui-monospace,SFMono-Regular,Menlo,monospace}
  .takeaway{margin:34px 0;padding:22px;border-left:3px solid var(--color-accent);background:var(--color-surface)}.takeaway strong{display:block;margin-bottom:8px}.takeaway p{margin:0!important}
  .claim-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px;margin-top:22px}.claim-grid>div{padding:18px;border:1px solid var(--color-border);border-radius:12px}.claim-grid small{color:var(--color-accent);font-size:.7rem;font-weight:800;letter-spacing:.1em}.claim-grid h3{margin:8px 0 8px;font-size:1rem}.claim-grid p{margin:0;font-size:.9rem;line-height:1.6}
  .evidence-line{margin-top:44px!important;padding-top:18px;border-top:1px solid var(--color-border);font-size:.86rem!important}.evidence-line a{color:var(--color-accent);text-decoration:none}.evidence-line a:hover{text-decoration:underline}
  @media(max-width:820px){.series-nav{grid-template-columns:1fr}.metric-grid,.claim-grid{grid-template-columns:1fr 1fr}.metric-table .row{grid-template-columns:1fr}.metric-table .head{display:none}.note-topline{flex-direction:column}.result-note{padding-inline:18px}}
  @media(max-width:520px){.metric-grid,.claim-grid{grid-template-columns:1fr}}
</style>
