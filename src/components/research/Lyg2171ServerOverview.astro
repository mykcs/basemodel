---
import type { Locale } from '../../i18n';

interface Props { locale: Locale }
const { locale } = Astro.props;
const zh = locale === 'zh';
const t = (cn: string, en: string) => zh ? cn : en;

const userStorage = [
  { label: t('我', 'Me'), total: '65.8 GiB', share: '15.1%', width: '15.1%', tone: 'mine', owner: true },
  { label: t('用户一', 'User 1'), total: '107.8 GiB', share: '24.7%', width: '24.7%', tone: 1 },
  { label: t('用户二', 'User 2'), total: '22.2 GiB', share: '5.1%', width: '5.1%', tone: 2 },
  { label: t('用户三', 'User 3'), total: '15.3 GiB', share: '3.5%', width: '3.5%', tone: 3 },
  { label: t('用户四', 'User 4'), total: '7.81 GiB', share: '1.8%', width: '1.8%', tone: 4 },
  { label: t('用户五', 'User 5'), total: '3.14 GiB', share: '0.7%', width: '0.7%', tone: 5 },
  { label: t('用户六', 'User 6'), total: '<0.01 GiB', share: '<0.1%', width: '0%', tone: 6, tiny: true },
];

const refreshPrompt = t(
  `请执行一次“共享实验服务器科研资产整理、上传与空间回收”。

目标页面：https://basemodel-preview.vercel.app/research/seed-openevo/flow/server/
仓库：mykcs/basemodel

开始前，请完整读取并严格执行：
docs/agents/current/server-artifact-governance-and-reclaim-sop.md

这份 Markdown 是本任务的详细执行方案和验收标准。不要只给我一个计划；凡是当前权限和既有 policy 已经允许、且不需要新的科学/可见性/不可逆人类决策的步骤，请实际执行到闭环。

你将依次完成：
1. 读取当前服务器/项目 authority，做低 I/O 的 live 空间与活跃引用盘点；
2. 对准备触碰的每个对象证明 ownership / lineage。OpenEvo、checkpoint、容器或镜像“名字看起来像我们的”一律不算证据；归属不闭合就 HOLD；
3. 按现有 Run Manifest / artifact manifest 标准给有科学意义的 run、checkpoint、trajectory、analysis/report、evaluation output、runtime 等补齐“身份证”，并输出 coverage receipt；
4. 查重后按语义上传/复用：代码、配置、small evidence/manifest → GitHub；checkpoint/adapter → Hugging Face model repo；大 trajectory/analysis data → 合适的 HF dataset/private staging；OCI runtime → GHCR；W&B 只作为 telemetry/cross-link，不当唯一备份；
5. 对每个远端对象记录 immutable revision/digest/hash，并做 fresh read/download/reload/restore 验证；上传成功本身不等于可删除；
6. 把本地对象分成 P0_ACTIVE / P1_ANALYSIS_HOLD / P2_ARCHIVE_THEN_RECLAIM / P3_REBUILDABLE / X_SHARED_OR_UNKNOWN，并保护 active、analysis hold、hot-recovery、shared/unknown 对象；
7. 生成 exact reclaim manifest（初始 NOT_AUTHORIZED）和技术版/通俗版报告。未知删除清单不被这条 Prompt 预授权；到这个唯一不可逆 gate 时，请让我批准 exact manifest；批准后重新检查 liveness/ownership，再精确删除，禁止 shared Docker broad prune；
8. 删除后分别记录 logical deleted bytes 和 df 实测变化；最后刷新中英文 BaseModel server 页面，若跨用户归属无法在不越权的情况下完整重算，就只刷新全盘 current snapshot，并把最近一次完整匿名归属快照明确标成历史，不允许用部分数据伪装成新排行；
9. 跑 deterministic tests、build、overflow/UI/browser acceptance，开 PR，验 exact-head Preview，合并并检查最终 Vercel 页面。

最终请交付：inventory/ownership 汇总、身份证覆盖率、GitHub/HF/GHCR 发布与复用清单、每个远端 immutable revision/digest + restore 结果、protected/HOLD 清单、exact reclaim manifest 与实际回收字节、更新后的服务器快照、PR/merge commit/Preview/Production 验收结果。`,
  `Run one end-to-end shared experiment-server artifact governance, publication, and space-reclaim pass.

Target page: https://basemodel-preview.vercel.app/research/seed-openevo/flow/server/
Repository: mykcs/basemodel

Before acting, read and strictly follow:
docs/agents/current/server-artifact-governance-and-reclaim-sop.md

That Markdown is the detailed execution plan and acceptance contract. Do not stop at a plan: execute every step already authorized by current policy until a genuine scientific, visibility, authority, or irreversible human gate is reached.

You will:
1. read current authority and take a low-I/O live storage/liveness snapshot;
2. prove ownership/lineage for every object you may touch. A name containing OpenEvo, checkpoint, a familiar container/image tag, or a project-like path is zero ownership evidence; unresolved ownership is HOLD;
3. build or repair canonical run/artifact passports for meaningful runs, checkpoints, trajectories, analysis/reports, evaluation outputs, runtimes, and related evidence, then report passport coverage;
4. deduplicate and route artifacts by semantics: code/config/small evidence/manifests to GitHub; checkpoints/adapters to a Hugging Face model repo; large trajectories/analysis data to an appropriate HF dataset/private staging area; OCI runtimes to GHCR; W&B is telemetry/cross-linking, not the only backup;
5. record immutable remote revisions/digests/hashes and perform fresh read/download/reload/restore verification. Upload success alone never makes a local copy deletable;
6. classify local material as P0_ACTIVE / P1_ANALYSIS_HOLD / P2_ARCHIVE_THEN_RECLAIM / P3_REBUILDABLE / X_SHARED_OR_UNKNOWN and protect active, analysis-hold, hot-recovery, shared, and unknown state;
7. produce an exact reclaim manifest (initially NOT_AUTHORIZED) plus technical and plain-language reports. This prompt does not pre-authorize an unknown deletion list. Stop only at that irreversible gate for approval of the exact manifest; after approval, re-check liveness/ownership and delete precisely. Never use broad shared-Docker prune;
8. record logical deleted bytes separately from the observed df change, then refresh both BaseModel server locales. If cross-user attribution cannot be recomputed completely without crossing authority/privacy boundaries, refresh the current global snapshot and keep the last complete anonymous attribution explicitly historical rather than fabricating a partial new ranking;
9. run deterministic tests, build, overflow/UI/browser acceptance, open a PR, verify the exact-head Preview, merge, and verify the final Vercel page.

Final delivery: inventory/ownership summary, passport coverage, GitHub/HF/GHCR publish/reuse matrix, immutable remote revisions/digests plus restore results, protected/HOLD set, exact reclaim manifest and actual reclaimed bytes, refreshed server snapshot, PR/merge commit, Preview, and Production acceptance.`
);

const gpuFacts = [
  { k: t('GPU 数量', 'GPU count'), v: '8 × RTX 5090' },
  { k: t('代际 / 架构', 'Generation / architecture'), v: 'GeForce RTX 50 · Blackwell' },
  { k: t('CUDA 核', 'CUDA cores'), v: '21,760 / GPU · 174,080 total' },
  { k: t('显存', 'VRAM'), v: '32,607 MiB / GPU · ≈ 254.7 GiB total' },
  { k: 'Compute Capability', v: '12.0' },
  { k: t('驱动快照', 'Driver snapshot'), v: '610.57.04' },
];
---
<main class="server-overview" data-locale={locale} data-storage-responsibility-view="owner-relative-historical-v2">
  <header class="server-hero">
    <h1>{t('实验服务器简介', 'Experiment server overview')}</h1>
    <p>{t('2026-09-05 00:49（UTC+8）的安全快照显示，这台机器仍为 2 × Intel Xeon Platinum 8380、约 1.0 TiB 内存，并可见 8 × RTX 5090。主文件系统当前约有 16.1 GiB 可用、df 报 97% 使用率；磁盘空间仍是最紧张的资源。', 'The safe 2026-09-05 00:49 (UTC+8) snapshot still shows 2 × Intel Xeon Platinum 8380 CPUs, about 1.0 TiB RAM, and 8 visible RTX 5090 GPUs. The main filesystem has about 16.1 GiB available and df reports 97% used, so disk capacity remains the tightest resource.')}</p>
    <div class="snapshot-note">
      <strong>{t('数据时间', 'Data timestamp')}</strong>
      <span>2026-09-05 00:49 (UTC+8)</span>
      <span>{t('静态快照，不是实时监控面板', 'Static snapshot, not a live monitoring dashboard')}</span>
    </div>
  </header>

  <section class="priority-grid" aria-label={t('关键资源', 'Key resources')}>
    <article class="priority-card priority-card--attention">
      <span>{t('磁盘剩余', 'Disk free')}</span>
      <strong>16.1 GiB</strong>
      <h2>{t('主文件系统可用空间', 'Main filesystem available')}</h2>
      <p>{t('本次全盘快照中，df 报告 97% 已使用。当前约有 16.1 GiB 普通可写空间，因此整理、归档与精确 reclaim 的优先级很高。', 'In this global snapshot, df reports 97% used with about 16.1 GiB available to ordinary writes, so artifact governance, archival, and precise reclaim are high priority.')}</p>
    </article>
    <article class="priority-card">
      <span>GPU</span>
      <strong>8 × 5090</strong>
      <h2>GeForce RTX 50 · Blackwell</h2>
      <p>{t('每张约 31.8 GiB 显存。8 张卡的显存不能自动当成一张 254.7 GiB 的大卡，是否能合并使用取决于并行策略。', 'Each GPU has about 31.8 GiB VRAM. Eight GPUs do not automatically behave like one 254.7 GiB GPU; usable aggregate memory depends on the parallelism strategy.')}</p>
    </article>
    <article class="priority-card">
      <span>CPU</span>
      <strong>80 / 160</strong>
      <h2>{t('80 个物理核 · 160 个逻辑线程', '80 physical cores · 160 logical threads')}</h2>
      <p>2 × Intel Xeon Platinum 8380 @ 2.30 GHz · 40 {t('核/路', 'cores/socket')} · 2 {t('线程/核', 'threads/core')}</p>
    </article>
    <article class="priority-card">
      <span>RAM</span>
      <strong>1.0 TiB</strong>
      <h2>{t('内存容量', 'System memory')}</h2>
      <p>{t('快照时约 84.1 GiB 正在使用，Linux 报告约 884.0 GiB available；RAM 仍远比磁盘宽裕。', 'At the snapshot, about 84.1 GiB was in use and Linux reported about 884.0 GiB available; RAM remains far less constrained than disk.')}</p>
    </article>
  </section>

  <section class="server-section">
    <div class="section-heading">
      <span>01</span>
      <div>
        <small>{t('存储', 'Storage')}</small>
        <h2>{t('磁盘使用情况', 'Filesystem usage')}</h2>
      </div>
    </div>
    <p class="section-lede">{t('主文件系统总容量约 436.0 GiB。2026-09-05 00:49（UTC+8）实测已用约 397.7 GiB，df 报 97% 使用率，普通写入约剩 16.1 GiB。下方归属条仍使用 2026-09-03 01:11（UTC+8）最近一次完整快照：我固定放在最前，其余账户按可归属容量从大到小排列。', 'The main filesystem has about 436.0 GiB total capacity. At 2026-09-05 00:49 (UTC+8), about 397.7 GiB was used, df reported 97%, and about 16.1 GiB remained available to ordinary writes. The attribution bar below still uses the most recent complete snapshot from 2026-09-03 01:11 (UTC+8): Me is pinned first, then the remaining accounts are ordered by attributable usage from largest to smallest.')}</p>

    <div class="storage-status">
      <div class="storage-status__numbers">
        <div><span>{t('文件系统', 'Filesystem')}</span><strong>436.0 GiB</strong></div>
        <div><span>{t('已用', 'Used')}</span><strong>397.7 GiB</strong></div>
        <div><span>{t('可用', 'Available')}</span><strong>16.1 GiB</strong></div>
        <div><span>df Use%</span><strong>97%</strong></div>
      </div>

      <div class="storage-attribution-head">
        <div><strong>{t('空间归属', 'Storage attribution')}</strong><span>2026-09-03 01:11 (UTC+8)</span></div>
        <div class="storage-attribution-owner"><strong>{t('我', 'Me')}</strong><span>65.8 GiB · 15.1%</span></div>
      </div>
      <div class="storage-bar storage-bar--attribution" role="img" aria-label={t('2026-09-03 01:11（UTC+8）完整空间归属：我 65.8 GiB，占 15.1%；用户一 107.8 GiB；用户二 22.2 GiB；用户三 15.3 GiB；用户四 7.81 GiB；用户五 3.14 GiB；用户六小于 0.01 GiB；共享/系统 147.7 GiB；未归属 Docker 19.0 GiB；当时可用 25.1 GiB；文件系统保留/舍入 22.2 GiB。', 'Complete storage attribution at 2026-09-03 01:11 (UTC+8): Me 65.8 GiB, 15.1%; User 1 107.8 GiB; User 2 22.2 GiB; User 3 15.3 GiB; User 4 7.81 GiB; User 5 3.14 GiB; User 6 under 0.01 GiB; shared/system 147.7 GiB; unattributed Docker 19.0 GiB; 25.1 GiB available then; 22.2 GiB filesystem reserve/rounding.')}>
        {userStorage.map((item) => (
          <span
            class:list={['storage-bar__account', `storage-bar__account--${item.tone}`, item.tiny && 'storage-bar__account--tiny']}
            style={`width:${item.width}`}
            title={`${item.label} · ${item.total} · ${item.share}`}
          ><b>{item.label}</b></span>
        ))}
        <span class="storage-bar__shared" style="width:33.9%" title={t('共享 / 系统 · 147.7 GiB · 33.9%', 'Shared / system · 147.7 GiB · 33.9%')}><b>{t('共享 / 系统', 'Shared / system')}</b></span>
        <span class="storage-bar__docker" style="width:4.4%" title={t('未归属 Docker · 19.0 GiB · 4.4%', 'Unattributed Docker · 19.0 GiB · 4.4%')}></span>
        <span class="storage-bar__free" style="width:5.7%" title={t('当时可用 · 25.1 GiB · 5.7%', 'Available then · 25.1 GiB · 5.7%')}></span>
        <span class="storage-bar__reserved" style="width:5.1%" title={t('文件系统保留 / 舍入 · 22.2 GiB · 5.1%', 'Filesystem reserve / rounding · 22.2 GiB · 5.1%')}></span>
      </div>
      <p class="storage-order-note">{t('我固定在最前；其余用户按可归属容量从大到小。小分段的精确数字保留在悬停提示与图表的可访问描述里，不再铺成一整片卡片。', 'Me is pinned first; the remaining users are sorted by attributable usage from largest to smallest. Exact values for small segments remain in hover text and the chart’s accessible description instead of being repeated as a large card grid.')}</p>
      <div class="why-df">
        <strong>{t('本轮精确回收', 'Precise reclaim in this pass')}</strong>
        <p>{t('经 storage-reclaim-20260905-v1 明确授权后，3 个已确认无活跃引用、且 exact Git commit 可从 GitHub 恢复的旧 linked worktree 已用 Git worktree 机制移除：逻辑删除 392,462,336 bytes；同期 df Available 净增加 389,718,016 bytes。第 4 个候选因仍被一个 shell 的 cwd 占用而自动 HOLD，没有删除。', 'After explicit approval of storage-reclaim-20260905-v1, three old linked worktrees with no live references and exact GitHub-recoverable commits were removed through Git worktree: 392,462,336 logical bytes deleted, while df Available increased by 389,718,016 bytes over the same interval. The fourth candidate was automatically held because a live shell still had its cwd there, so it was not deleted.')}</p>
      </div>
      <div class="why-df">
        <strong>{t('归属不能靠名字猜', 'Ownership cannot be guessed from names')}</strong>
        <p>{t('这台服务器上多人同时做 OpenEvo。文件名、run 名、容器名、Docker tag 或“看起来像某个实验”的路径都只是线索，不是 ownership 证据。清理前必须把 filesystem / run manifest / Git / container reference / artifact digest 等证据闭合；归属不清就 HOLD。', 'Multiple people run OpenEvo on this server. File names, run names, container names, Docker tags, or paths that look like a particular experiment are clues, not ownership evidence. Before cleanup, filesystem, run-manifest, Git, container-reference, artifact-digest, and related provenance must close the loop; unresolved ownership is HOLD.')}</p>
      </div>
    </div>
  </section>

  <section class="server-section">
    <div class="section-heading">
      <span>02</span>
      <div>
        <small>GPU</small>
        <h2>{t('GPU 规格', 'GPU specifications')}</h2>
      </div>
    </div>
    <p class="section-lede">{t('RTX 5090 属于 GeForce RTX 50 系列，采用 Blackwell 架构。单卡 21,760 个 CUDA 核，显存 32,607 MiB，Compute Capability 为 12.0。', 'RTX 5090 belongs to the GeForce RTX 50 series and uses the Blackwell architecture. Each GPU has 21,760 CUDA cores, 32,607 MiB VRAM, and Compute Capability 12.0.')}</p>
    <dl class="gpu-facts">
      {gpuFacts.map((item) => <div><dt>{item.k}</dt><dd>{item.v}</dd></div>)}
    </dl>
    <div class="gpu-note">
      <strong>{t('多卡性能说明', 'Multi-GPU performance note')}</strong>
      <p>{t('8 张卡 × 21,760 CUDA 核可以得到整机 174,080 CUDA 核这个“规格总和”，但训练速度不会简单变成单卡的 8 倍。数据并行、模型并行、通信和显存分配都会决定实际收益。', 'Eight cards × 21,760 CUDA cores gives a hardware-spec total of 174,080 CUDA cores, but training does not automatically become eight times faster. Data/model parallelism, communication, and memory placement determine the real gain.')}</p>
    </div>
  </section>

  <section class="server-section">
    <div class="section-heading">
      <span>03</span>
      <div>
        <small>{t('快捷操作', 'Quick action')}</small>
        <h2>{t('整理、上传与清理服务器空间', 'Govern, publish, and reclaim server artifacts')}</h2>
      </div>
    </div>
    <p class="section-lede">{t('当你准备整理服务器时，复制下面这段 Prompt 到一个能够操作服务器与相关远端仓库的 ChatGPT / Codex 对话。它会先读取一份详细 Markdown，然后按“盘点 → 身份证 → 上传/验证 → 精确 reclaim → 网页刷新”执行；未知删除清单仍只在 exact manifest 处向你确认一次。', 'When you are ready to organize the server, copy this prompt into a ChatGPT/Codex conversation that can operate the server and relevant remotes. It first reads a detailed Markdown contract, then runs inventory → passports → publish/verify → precise reclaim → webpage refresh; an unknown deletion list still pauses once at the exact-manifest approval gate.')}</p>
    <div class="prompt-summary" aria-label={t('这次会做什么', 'What this workflow does')}>
      <div><strong>{t('整理', 'Organize')}</strong><span>{t('盘点、归属证明、P0/P1/P2/P3/X 分类', 'Inventory, ownership proof, P0/P1/P2/P3/X classification')}</span></div>
      <div><strong>{t('身份证', 'Passport')}</strong><span>{t('Run Manifest + artifact digest / lineage / restore 信息', 'Run Manifest + artifact digest / lineage / restore identity')}</span></div>
      <div><strong>{t('上传', 'Publish')}</strong><span>{t('GitHub / Hugging Face / GHCR 按语义路由并 fresh verify', 'Route to GitHub / Hugging Face / GHCR and fresh-verify')}</span></div>
      <div><strong>{t('清理', 'Reclaim')}</strong><span>{t('exact manifest 授权后精确删除，并实测 df', 'Precise deletion after exact-manifest approval, then measured df')}</span></div>
    </div>
    <div class="refresh-prompt">
      <pre data-copy-label={t('复制服务器整理 Prompt', 'Copy server-governance prompt')}><code>{refreshPrompt}</code></pre>
    </div>
  </section>

  <section class="server-section server-section--privacy">
    <div class="section-heading">
      <span>04</span>
      <div>
        <small>{t('隐私边界', 'Privacy boundary')}</small>
        <h2>{t('资源归属与隐私', 'Usage attribution and privacy')}</h2>
      </div>
    </div>
    <div class="privacy-grid">
      <div>
        <strong>{t('公开展示', 'Shown publicly')}</strong>
        <p>{t('2026-09-05 00:49（UTC+8）的全盘容量、已用/可用空间与硬件库存，以及单独标注为 2026-09-03 01:11（UTC+8）的最近一次完整归属快照。站点 owner 只显示为相对标签“我”，其他账户继续匿名。', 'The live-safe 2026-09-05 00:49 (UTC+8) global capacity/hardware snapshot plus the separately dated most recent complete attribution snapshot from 2026-09-03 01:11 (UTC+8). The site owner appears only as the relative label “Me”; all other accounts remain anonymous.')}</p>
      </div>
      <div>
        <strong>{t('不公开', 'Not shown')}</strong>
        <p>{t('用户名、容器名、容器 ID、GPU 进程命令、GPU UUID、IP、SSH 别名、匿名编号映射或任何可反查身份的信息。', 'Usernames, container names/IDs, GPU process commands, GPU UUIDs, IPs, SSH aliases, anonymous-label mappings, or other identity-revealing details.')}</p>
      </div>
    </div>
  </section>

  <details class="measurement-notes">
    <summary>{t('数据来源', 'Data sources')}</summary>
    <p>{t('2026-09-05 current 全盘容量来自对主文件系统的 live df；CPU/RAM/GPU 库存也在同次安全审计中复核。跨用户 home 的完整匿名归属没有在今天强行重跑：不会为了网页统计去进入同学容器或利用 Docker 管理能力挂载同学目录，因此归属条保留为 2026-09-03 的最近一次完整快照，并只把站点 owner 显示为相对标签“我”。Docker 名称/标签永远不作为 ownership 证明。页面只保存公开安全的聚合/匿名数字，不保存用户名、SSH 地址、GPU UUID、容器名、容器 ID 或匿名编号映射。', 'The 2026-09-05 current global capacity comes from a live df of the main filesystem; CPU/RAM/GPU inventory was rechecked in the same safe audit. Complete cross-user home attribution was intentionally not forced today: the page refresh does not enter sibling-user containers or use Docker-admin capability to mount sibling homes, so the attribution bar remains the most recent complete snapshot from 2026-09-03 and exposes the site owner only as the relative label “Me”. Docker names/labels are never ownership proof. The page stores only public-safe aggregate/anonymous figures, not usernames, SSH endpoints, GPU UUIDs, container names/IDs, or anonymous-label mappings.')}</p>
  </details>
</main>

<style>
  .server-overview{color:var(--ink)}
  .server-hero{max-width:930px;padding:clamp(1.4rem,4vw,3rem) 0 1.35rem}
  .server-hero h1{max-width:18ch;margin:0 0 .8rem;font-size:clamp(2rem,5vw,4.2rem);line-height:.98;letter-spacing:-.055em}
  .server-hero>p{max-width:72ch;margin:0;color:var(--muted);font-size:1.02rem;line-height:1.75}
  .snapshot-note{display:flex;flex-wrap:wrap;gap:.45rem .75rem;align-items:center;margin-top:1rem;color:var(--muted);font-size:.72rem}
  .snapshot-note strong{color:var(--ink)}
  .snapshot-note span+span{padding-left:.75rem;border-left:1px solid var(--line)}

  .priority-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:.75rem;margin:1rem 0 3.5rem}
  .priority-card{min-height:220px;padding:1.15rem;border:1px solid var(--line);border-radius:18px;background:var(--surface)}
  .priority-card--attention{border-color:color-mix(in srgb,var(--accent-deep) 48%,var(--line));background:color-mix(in srgb,var(--accent) 8%,var(--surface))}
  .priority-card>span{display:block;color:var(--muted);font:800 .62rem/1.2 var(--font-mono);letter-spacing:.06em;text-transform:uppercase}
  .priority-card>strong{display:block;margin:.8rem 0 .3rem;font-size:clamp(1.85rem,3vw,3rem);line-height:1;letter-spacing:-.055em}
  .priority-card h2{margin:.2rem 0 .75rem;font-size:.9rem;line-height:1.35}
  .priority-card p{margin:0;color:var(--muted);font-size:.78rem;line-height:1.65}

  .server-section{padding:2.4rem 0;border-top:1px solid var(--line)}
  .section-heading{display:flex;gap:.8rem;align-items:flex-start;margin-bottom:1rem}
  .section-heading>span{padding-top:.2rem;color:var(--accent-deep);font:800 .68rem/1 var(--font-mono)}
  .section-heading small{display:block;color:var(--muted);font:800 .62rem/1.3 var(--font-mono);letter-spacing:.06em;text-transform:uppercase}
  .section-heading h2{margin:.24rem 0 0;font-size:clamp(1.35rem,2.7vw,2.2rem);letter-spacing:-.035em}
  .section-lede{max-width:76ch;margin:0 0 1.35rem;color:var(--muted);line-height:1.75}

  .storage-status{padding:1.15rem;border:1px solid var(--line);border-radius:18px;background:var(--surface)}
  .storage-status__numbers{display:grid;grid-template-columns:repeat(4,1fr);gap:.6rem;margin-bottom:1rem}
  .storage-status__numbers>div{padding:.8rem;border-radius:12px;background:color-mix(in srgb,var(--ink) 3%,transparent)}
  .storage-status__numbers span,.storage-status__numbers strong{display:block}
  .storage-status__numbers span{color:var(--muted);font-size:.7rem}
  .storage-status__numbers strong{margin-top:.2rem;font-size:1.35rem;letter-spacing:-.035em}
  .storage-bar{display:flex;height:30px;overflow:hidden;border:1px solid var(--line);border-radius:9px;background:color-mix(in srgb,var(--ink) 3%,transparent)}
  .storage-bar span{display:block;height:100%}
  .storage-bar__docker{background:color-mix(in srgb,var(--accent-deep) 52%,var(--surface))}
  .storage-bar__shared{background:color-mix(in srgb,var(--ink) 48%,var(--surface))}
  .storage-bar__free{background:color-mix(in srgb,var(--accent) 18%,var(--surface))}
  .storage-bar__reserved{background:repeating-linear-gradient(135deg,color-mix(in srgb,var(--ink) 15%,var(--surface)) 0 4px,transparent 4px 8px)}
  .storage-bar--attribution{height:42px;margin-top:.15rem}
  .storage-bar .storage-bar__account,.storage-bar .storage-bar__shared{display:flex;align-items:center;justify-content:center;min-width:0}
  .storage-bar .storage-bar__account{border-right:1px solid color-mix(in srgb,var(--surface) 38%,transparent)}
  .storage-bar__account b,.storage-bar__shared b{overflow:hidden;padding:0 .22rem;font:800 .56rem/1 var(--font-mono);letter-spacing:-.02em;white-space:nowrap;text-overflow:clip}
  .storage-bar__account--mine{background:var(--accent-deep)}
  .storage-bar__account--mine b{color:var(--on-accent)}
  .storage-bar__account--1{background:color-mix(in srgb,var(--accent-deep) 76%,var(--surface))}
  .storage-bar__account--2{background:color-mix(in srgb,var(--accent-deep) 60%,var(--surface))}
  .storage-bar__account--3{background:color-mix(in srgb,var(--accent-deep) 48%,var(--surface))}
  .storage-bar__account--4{background:color-mix(in srgb,var(--accent-deep) 38%,var(--surface))}
  .storage-bar__account--5{background:color-mix(in srgb,var(--accent-deep) 28%,var(--surface))}
  .storage-bar__account--6{border-right:0;border-left:1px solid var(--accent-deep);background:transparent}
  .storage-bar__account--1 b,.storage-bar__account--2 b{color:var(--ink)}
  .storage-bar__account--3 b,.storage-bar__account--4 b,.storage-bar__account--5 b,.storage-bar__account--6 b{display:none}
  .storage-bar__shared b{color:var(--surface)}
  .storage-bar__account--tiny b{display:none}
  .storage-attribution-head{display:flex;flex-wrap:wrap;align-items:end;justify-content:space-between;gap:.55rem 1rem;margin-bottom:.7rem}
  .storage-attribution-head>div:first-child strong,.storage-attribution-head>div:first-child span{display:block}
  .storage-attribution-head>div:first-child strong{font-size:.82rem}
  .storage-attribution-head>div:first-child span{margin-top:.15rem;color:var(--muted);font:700 .66rem/1.4 var(--font-mono)}
  .storage-attribution-owner{display:flex;align-items:baseline;gap:.5rem}
  .storage-attribution-owner strong{color:var(--accent-deep);font-size:.78rem}
  .storage-attribution-owner span{font-size:.92rem;font-weight:800;letter-spacing:-.02em}
  .storage-order-note{margin:.55rem 0 0;color:var(--muted);font-size:.68rem;line-height:1.55}
  .why-df{margin-top:1rem;padding:.9rem 1rem;border-left:3px solid var(--accent-deep);background:color-mix(in srgb,var(--accent) 6%,transparent)}
  .why-df strong{font-size:.8rem}
  .why-df p{margin:.25rem 0 0;color:var(--muted);font-size:.76rem;line-height:1.65}

  .gpu-facts{display:grid;grid-template-columns:repeat(3,1fr);gap:.65rem;margin:1.1rem 0 0}
  .gpu-facts>div{padding:.95rem 1rem;border:1px solid var(--line);border-radius:14px;background:var(--surface)}
  .gpu-facts dt{color:var(--muted);font-size:.68rem}
  .gpu-facts dd{margin:.22rem 0 0;font-size:.9rem;font-weight:760;line-height:1.45}
  .gpu-note{margin-top:.85rem;padding:1rem;border:1px solid var(--line);border-radius:14px}
  .gpu-note strong{font-size:.8rem}
  .gpu-note p{margin:.3rem 0 0;color:var(--muted);font-size:.78rem;line-height:1.65}

  .prompt-summary{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:.6rem;margin:0 0 1rem}
  .prompt-summary>div{padding:.8rem;border:1px solid var(--line);border-radius:12px;background:var(--surface)}
  .prompt-summary strong,.prompt-summary span{display:block}.prompt-summary strong{font-size:.78rem}.prompt-summary span{margin-top:.25rem;color:var(--muted);font-size:.68rem;line-height:1.55}
  .refresh-prompt{max-width:100%}
  .refresh-prompt pre{max-height:32rem;overflow:auto;margin:0;padding:1rem;border:1px solid var(--line);border-radius:10px;background:color-mix(in srgb,var(--ink) 3%,var(--surface));white-space:pre-wrap;overflow-wrap:anywhere;font-size:.72rem;line-height:1.65}
  .refresh-prompt code{font:inherit;color:inherit}

  .privacy-grid{display:grid;grid-template-columns:1fr 1fr;gap:.75rem}
  .privacy-grid>div{padding:1rem;border:1px solid var(--line);border-radius:14px;background:var(--surface)}
  .privacy-grid strong{font-size:.84rem}
  .privacy-grid p{margin:.35rem 0 0;color:var(--muted);font-size:.78rem;line-height:1.65}
  .measurement-notes{margin:1rem 0 0;padding:1rem;border:1px solid var(--line);border-radius:14px;background:var(--surface)}
  .measurement-notes summary{cursor:pointer;font-weight:760;font-size:.82rem}
  .measurement-notes p{margin:.7rem 0 0;color:var(--muted);font-size:.75rem;line-height:1.7}

  @media(max-width:960px){.priority-grid{grid-template-columns:1fr 1fr}.gpu-facts,.prompt-summary{grid-template-columns:1fr 1fr}}
  @media(max-width:640px){
    .server-hero h1{max-width:12ch}
    .priority-grid,.storage-status__numbers,.gpu-facts,.privacy-grid,.prompt-summary{grid-template-columns:1fr}
    .priority-card{min-height:0}
    .storage-bar{height:24px}
    .storage-bar--attribution{height:34px}
    .storage-bar__account--2 b{display:none}
    .storage-attribution-head{align-items:flex-start}
    .storage-attribution-owner{width:100%;justify-content:space-between;border-top:1px solid var(--line);padding-top:.5rem}
    .snapshot-note span+span{padding-left:0;border-left:0}
  }
</style>