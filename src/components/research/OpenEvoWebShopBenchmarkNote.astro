---
export type OpenEvoWebShopBenchmarkNoteId =
  | 'benchmark-first'
  | 'seed-faithful-benchmark'
  | 'openevo-benchmark-design';

interface Props { note: OpenEvoWebShopBenchmarkNoteId }
const { note } = Astro.props;
const root = '/research/seed-openevo/study/results';

const notes = [
  {
    id: 'benchmark-first',
    index: '10',
    title: '为什么还需要一场正式的 WebShop 比较？',
    description: '现有实验已经回答了一部分机制问题，也复现了第一代内部新任务迁移；真正缺失的是 OpenEvo 与 SEED 在同一套官方保留任务条件下的最终比较。',
  },
  {
    id: 'seed-faithful-benchmark',
    index: '11',
    title: '怎样才算和 SEED 公平比较？',
    description: '固定同一个 WebShop small world、任务划分、goal ordering、交互规则与评分方式，让方法差异成为主要变量。',
  },
  {
    id: 'openevo-benchmark-design',
    index: '12',
    title: 'OpenEvo 的不同进化方式应该怎样比较？',
    description: '把“经验以什么形式留下来”和“训练时使用什么反馈”拆成两个轴，在正式比较之后逐步做方法消融。',
  },
] as const;

const current = notes.find((item) => item.id === note)!;
---

<article class="benchmark-note" data-ui-audit="contrast layout overflow">
  <header class="note-hero">
    <div class="note-topline">
      <a href={`${root}/`}>← 返回研究结果</a>
      <span>OPENEVO × WEBSHOP · BENCHMARK DESIGN {current.index}</span>
    </div>
    <h1>{current.title}</h1>
    <p class="dek">{current.description}</p>
    <p class="status"><strong>状态：</strong>实验设计中，尚未产生正式比较结果。</p>
  </header>

  <nav class="series-nav" aria-label="OpenEvo WebShop benchmark design">
    {notes.map((item) => (
      <a class:list={[{ active: item.id === note }]} href={`${root}/${item.id}/`}>
        <span>{item.index}</span>{item.title}
      </a>
    ))}
  </nav>

  {note === 'benchmark-first' && (
    <main class="note-body">
      <section class="opening-section">
        <p class="section-kicker">01 · 已经知道什么</p>
        <h2>OpenEvo 已经有第一代内部迁移证据，但这还不是 SEED 官方保留任务结果。</h2>
        <p>H1.38B 首次观察到可靠的 internal fresh-task transfer（内部新任务迁移），H1.39 又在独立 task-ID-disjoint panel（任务 ID 不重叠评估组）上复现。两组正式正向迁移任务都来自 <strong>goal_idx ≥ 500</strong>。</p>
        <p>因此，这些实验支持“第一代内部新任务迁移可以出现并复现”，但不能回答 OpenEvo 在 SEED official held-out tasks（SEED 官方保留任务）0–499 上是否有效。</p>
      </section>

      <section>
        <p class="section-kicker">02 · 缺少什么</p>
        <h2>正式的 OpenEvo vs SEED 比较目前没有结果。</h2>
        <div class="question-card">
          <p>H1.30</p>
          <strong>formal evaluation denominator（正式评估分母）= 0</strong>
        </div>
        <p>H1.30 验证过 SEED route、parquet、trainer / checkpoint 等基础设施，但正式比较没有启动，也没有消费正式评估任务。因此当前不存在一组可以解释为“OpenEvo 相对 SEED 的最终成绩”的旧结果。</p>
      </section>

      <section>
        <p class="section-kicker">03 · 两类问题</p>
        <h2>最终效果和作用机制需要分别回答。</h2>
        <div class="lane-grid">
          <div class="lane primary">
            <small>BENCHMARK</small>
            <h3>在同一套规则下，OpenEvo 最终有没有更强？</h3>
            <p>比较 frozen agent（冻结后的智能体）在同一批 SEED 官方保留任务上的 Task Score 与 exact Success。</p>
          </div>
          <div class="lane">
            <small>MECHANISM</small>
            <h3>为什么会提高，或者为什么没有提高？</h3>
            <p>经验供给、参数写入、fresh-task transfer、retention、continual integration 与 multi-generation 等实验用于解释主结果。</p>
          </div>
        </div>
      </section>

      <section>
        <p class="section-kicker">04 · 下一项正式证据</p>
        <h2>只需要补一场 SEED-compatible fair comparison（与 SEED 严格兼容的公平比较）。</h2>
        <p>训练任务继续来自 500–6909；正式评估只使用 0–499；OpenEvo 与 SEED 使用相同任务身份、environment seed、交互规则和评分协议。现有 H0–H1.41 机制实验不需要因此全部重跑。</p>
      </section>

      <footer class="next-note"><a href={`${root}/seed-faithful-benchmark/`}>下一问：怎样才算和 SEED 公平比较？ →</a></footer>
    </main>
  )}

  {note === 'seed-faithful-benchmark' && (
    <main class="note-body">
      <section class="opening-section">
        <p class="section-kicker">01 · 评价单位</p>
        <h2>WebShop 的基本评价单位是一整个 shopping episode（购物任务回合）。</h2>
        <p>instruction 固定以后，模型每一步动作都会改变下一步 observation（观察）。最终评价的是 frozen policy（冻结策略）从任务开始到终止的 closed-loop behavior（闭环行为），而不是某一个 action token。</p>
        <pre><code>{`Instruction
   ↓
Observation → Action
   ↓          ↓
new page  ← environment
   ↓
重复交互
   ↓
Buy / terminal
   ↓
Task Score + exact Success`}</code></pre>
      </section>

      <section>
        <p class="section-kicker">02 · 数据边界</p>
        <h2>训练区和正式保留任务必须在实验开始前分开。</h2>
        <div class="two-column">
          <div>
            <small>训练候选池 (TRAIN CANDIDATE POOL)</small>
            <h3>goal 500–6909</h3>
            <p>训练、经验采集、内部 validation（验证）与方法选择发生在这个范围。</p>
          </div>
          <div>
            <small>正式保留池 (OFFICIAL HELD-OUT POOL)</small>
            <h3>goal 0–499</h3>
            <p>正式比较只从这个范围取任务，而且 OpenEvo 与 SEED 必须使用同一批冻结任务身份。</p>
          </div>
        </div>
      </section>

      <section>
        <p class="section-kicker">03 · Goal ordering</p>
        <h2>任务编号只有在 environment seed 一致时才真正可比。</h2>
        <p>WebShop 会在环境初始化时 shuffle goals（打乱任务顺序）。如果目标是与 SEED GRPO 的 exact-ID setting（精确任务编号设定）对齐，应使用 <strong>environment seed = 0</strong>，并保存每个任务的 goal_idx、instruction hash 与数据版本。</p>
      </section>

      <section>
        <p class="section-kicker">04 · 评分</p>
        <h2>连续任务分数、精确成功和训练 reward 不能混成一个指标。</h2>
        <div class="protocol-table" role="table" aria-label="WebShop evaluation metrics">
          <div class="row head" role="row"><span>指标</span><span>含义</span></div>
          <div class="row" role="row"><span>原始任务分数 (task_score)</span><strong>WebShop 原始连续任务分数</strong></div>
          <div class="row" role="row"><span>精确成功 (exact success)</span><strong>原始 reward = 1.0 时的完整成功</strong></div>
          <div class="row" role="row"><span>训练 reward (SEED training reward)</span><strong>成功映射为 10，否则为 0；只用于训练语义</strong></div>
        </div>
        <p>正式结果同时报告 raw task_score（原始任务分数）和 exact success（精确成功），这样不同方法的结果可以在同一尺度上核对。</p>
      </section>

      <section>
        <p class="section-kicker">05 · 测试时行为</p>
        <h2>正式评估开始以后，模型状态必须保持冻结。</h2>
        <p>不同 held-out tasks（保留任务）之间不更新 memory、adapter 或 policy。否则前一个测试任务会变成后一个任务的训练信息，最终结果就不再表示同一个 frozen agent 在未见任务上的表现。</p>
      </section>

      <section>
        <p class="section-kicker">06 · 可追溯身份</p>
        <h2>每个正式结果都需要能够还原“谁在什么任务上、用什么数据跑出来”。</h2>
        <p>至少保存 goal_idx、instruction hash、数据文件 SHA256、environment seed、代码 SHA、模型 / adapter 身份、raw task_score 与 exact success。OpenEvo 和 SEED 的比较任务身份必须逐项匹配。</p>
      </section>

      <footer class="next-note"><a href={`${root}/openevo-benchmark-design/`}>下一问：OpenEvo 的不同进化方式怎样比较？ →</a></footer>
    </main>
  )}

  {note === 'openevo-benchmark-design' && (
    <main class="note-body">
      <section class="opening-section">
        <p class="section-kicker">01 · 两个变量</p>
        <h2>“进化什么”和“用什么反馈进化”是两个不同问题。</h2>
        <div class="axis-grid">
          <div>
            <small>AXIS A · CARRIER</small>
            <h3>Native ↔ Param</h3>
            <p>experience / strategy / memory（经验 / 策略 / 记忆）等外部持久状态，与进一步写入 policy 参数的 adapter 路线分开比较。</p>
          </div>
          <div>
            <small>AXIS B · FEEDBACK</small>
            <h3>Sparse ↔ Dense</h3>
            <p>只使用 exact success（精确成功），还是使用 WebShop 连续 partial task score（部分任务分数），作为独立实验条件。</p>
          </div>
        </div>
      </section>

      <section>
        <p class="section-kicker">02 · Native carrier</p>
        <h2>外部经验载体能否把过去任务中的规律带到后续任务？</h2>
        <p>这类状态可以包含搜索策略、variant 检查、价格约束、失败后的回退策略等经验。关键评价不是 memory 是否变长，而是冻结后的策略是否在新的任务上带来更高 Task Score / Success。</p>
      </section>

      <section>
        <p class="section-kicker">03 · Parametric carrier</p>
        <h2>把经验继续写进参数以后，迁移收益和旧能力保持会怎样变化？</h2>
        <p>SD-LoRA 与现有 parameter-write（参数写入）实验提供了直接机制证据。H1.38B/H1.39 说明第一代内部迁移可以出现；H1.40 则说明第二代 acquisition / retention / preservation（新知识获取 / 旧知识保持 / 已有收益保持）还没有同时稳定成立。</p>
      </section>

      <section>
        <p class="section-kicker">04 · Feedback density</p>
        <h2>Sparse 与 Dense feedback 应该作为单独的消融变量。</h2>
        <div class="lane-grid">
          <div class="lane primary">
            <small>SPARSE</small>
            <h3>fitness = exact success</h3>
            <p>只使用成功 / 失败，便于和 SEED 的稀疏训练 reward 语义对齐。</p>
          </div>
          <div class="lane">
            <small>DENSE</small>
            <h3>fitness = partial task score</h3>
            <p>利用连续环境反馈，检验 OpenEvo 是否能从未完全成功的轨迹中获得更细粒度信号。</p>
          </div>
        </div>
        <p>如果 Dense 高于 Sparse，结论应是“OpenEvo 能利用 graded environment feedback（分级环境反馈）”，而不是把两种监督条件混成一个方法优越性结论。</p>
      </section>

      <section>
        <p class="section-kicker">05 · 实验顺序</p>
        <h2>先完成官方保留任务公平比较，再扩大方法变体。</h2>
        <p>第一优先级是建立同一 frozen benchmark contract（冻结比较协议）下的正式 OpenEvo / SEED 结果。Native、Param、Sparse、Dense 等变体随后用于解释主结果，而不是替代这场缺失的正式比较。</p>
      </section>
    </main>
  )}
</article>

<style>
  .benchmark-note{--reading:820px;width:min(1180px,calc(100% - 2rem));margin:1rem auto 5rem;color:var(--color-text)}
  .note-hero{max-width:960px;margin:0 auto;padding:54px 0 34px}.note-topline{display:flex;justify-content:space-between;gap:24px;margin-bottom:28px;color:var(--color-text-muted);font:700 .72rem/1.4 ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.06em}.note-topline a{color:var(--color-accent);text-decoration:none}.note-hero h1{max-width:900px;margin:0;font-size:clamp(2.35rem,5.2vw,5rem);line-height:1.01;letter-spacing:-.055em}.dek{max-width:780px;margin:24px 0 0;color:var(--color-text-muted);font-size:1.08rem;line-height:1.75}.status{max-width:780px;margin:20px 0 0;padding:12px 14px;border-left:3px solid var(--color-accent);background:var(--color-surface-muted);color:var(--color-text-muted);font-size:.88rem;line-height:1.6}.status strong{color:var(--color-text)}
  .series-nav{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));max-width:960px;margin:0 auto 54px;border-block:1px solid var(--color-border)}.series-nav a{display:grid;grid-template-columns:34px 1fr;gap:10px;padding:16px 14px;color:var(--color-text-muted);text-decoration:none;font-size:.82rem;line-height:1.45;border-right:1px solid var(--color-border)}.series-nav a:last-child{border-right:0}.series-nav a span{font:600 .72rem ui-monospace,SFMono-Regular,Menlo,monospace;color:var(--color-text-muted)}.series-nav a.active{background:var(--color-surface-muted);color:var(--color-text)}
  .note-body{width:min(var(--reading),100%);margin:0 auto}.note-body section{padding:32px 0 38px;border-top:1px solid var(--color-border)}.opening-section{border-top:0!important;padding-top:0!important}.section-kicker{margin:0 0 10px!important;color:var(--color-accent)!important;font:800 .7rem/1.4 ui-monospace,SFMono-Regular,Menlo,monospace!important;letter-spacing:.08em;text-transform:uppercase}.note-body h2{margin:0 0 18px;font-size:clamp(1.55rem,3vw,2.35rem);line-height:1.16;letter-spacing:-.035em}.note-body h3{margin:0 0 8px;font-size:1.08rem;line-height:1.4}.note-body p,.note-body li{color:var(--color-text);font-size:1rem;line-height:1.82}.note-body p{margin:0 0 16px}.note-body a{color:var(--color-accent)}
  .question-card{margin:24px 0;padding:22px;border:1px solid var(--color-border);border-radius:14px;background:var(--color-surface)}.question-card p{margin:0 0 8px;color:var(--color-text-muted);font:700 .72rem/1.4 ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.06em}.question-card strong{display:block;font-size:1.22rem;line-height:1.55}
  .two-column,.lane-grid,.axis-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px;margin:22px 0}.two-column>div,.lane,.axis-grid>div{padding:20px;border:1px solid var(--color-border);border-radius:14px;background:var(--color-surface)}.two-column small,.lane small,.axis-grid small{display:block;margin-bottom:9px;color:var(--color-accent);font:800 .68rem/1.4 ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.06em}.two-column p,.lane p,.axis-grid p{margin-bottom:0;color:var(--color-text-muted);font-size:.91rem;line-height:1.65}.lane.primary{border-color:color-mix(in srgb,var(--color-accent) 55%,var(--color-border))}
  pre{overflow:auto;margin:22px 0;padding:20px;border:1px solid var(--color-border);border-radius:12px;background:var(--color-surface-muted);color:var(--color-text);font-size:.84rem;line-height:1.65}code{font-family:ui-monospace,SFMono-Regular,Menlo,monospace}
  .protocol-table{margin:22px 0;border:1px solid var(--color-border);border-radius:14px;overflow:hidden}.protocol-table .row{display:grid;grid-template-columns:minmax(150px,.8fr) minmax(0,1.7fr);gap:18px;padding:13px 16px;border-bottom:1px solid var(--color-border);align-items:start}.protocol-table .row:last-child{border-bottom:0}.protocol-table .row.head{background:var(--color-surface-muted);color:var(--color-text-muted);font:700 .72rem/1.5 ui-monospace,SFMono-Regular,Menlo,monospace;text-transform:uppercase}.protocol-table .row span{font-size:.9rem;line-height:1.55}.protocol-table .row strong{font-size:.92rem;line-height:1.55}
  .next-note{padding-top:28px;border-top:1px solid var(--color-border)}.next-note a{font-weight:700;text-decoration:none}
  @media(max-width:760px){.benchmark-note{width:min(100% - 1rem,1180px)}.note-hero{padding-top:30px}.note-topline{flex-direction:column;gap:8px}.series-nav{grid-template-columns:1fr}.series-nav a{border-right:0;border-bottom:1px solid var(--color-border)}.series-nav a:last-child{border-bottom:0}.two-column,.lane-grid,.axis-grid{grid-template-columns:1fr}.protocol-table .row{grid-template-columns:1fr;gap:4px}}
</style>
