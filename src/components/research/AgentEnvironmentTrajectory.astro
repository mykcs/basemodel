---
import type { Locale } from '../../i18n';
import ResearchConceptIndex from './ResearchConceptIndex.astro';

interface Props {
  locale: Locale;
  context?: 'benchmark' | 'reproduction';
}

const { locale, context = 'benchmark' } = Astro.props;
---

<!-- Compatibility shell: full SEED/OpenEvo/WebShop/ALFWorld figures belong to their dedicated routes. -->
<ResearchConceptIndex locale={locale} context={context} />
