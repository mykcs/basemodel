@AGENTS.md

# Claude Code source-directory adapter

The imported `AGENTS.md` is the scoped rule for every change under `src/`. Apply it to new features, UI work, navigation, responsive changes, and copy changes alike. Detailed canonical policy remains under `docs/agents/current/`; do not duplicate it here.
