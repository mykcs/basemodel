# Vercel Preview retry marker

This file records a single retry of the responsive learning UI Preview after the prior Vercel account-level build-rate limit. It has no runtime effect and is intentionally prefixed with `[CF-Pages-Skip]` at commit time so Cloudflare is not used for this validation retry.
