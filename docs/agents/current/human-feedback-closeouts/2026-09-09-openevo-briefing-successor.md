# Human Preference Learning closeout — OpenEVO briefing successor — 2026-09-09

Authority: `INGESTION-20260909-OPENEVO-BRIEFING-SUCCESSOR` in `src/data/humanFeedbackIngestionCloseouts.ts`.

This is the chained successor to `INGESTION-20260909-OPENEVO-BRIEFING`. Recompute it with:

```bash
npm run feedback:ingestion-closeout -- INGESTION-20260909-OPENEVO-BRIEFING-SUCCESSOR
```

The successor ledger contains **8** identifiable post-merge feedback signals: 4 `ingest`, 1 `merge-duplicate`, 3 `task-fact-not-preference`, 0 unresolved holds. Together with the predecessor ledger, the closeout chain covers **37** identified signals without double-counting the checkpoint/build feedback in the earlier source window.

The page successor itself is **not accepted**. PR #594 / `93f7bcda…` is recorded only as `current-candidate`; no event or visual reference promotes it to `accepted`, `canonical`, or Golden. The explicit future-default language applies to the **review workflow** only: fast prebuilt Preview while iterating, exact-head final Vercel gate when merge-ready.

New reusable learning in this successor:

- diagnostic slides must expose observed behavior → ruled-out explanation → what we stop changing → next mechanism/question;
- continuous checkpoint/W&B evidence is useful, but optimization loss, training-process task score, and frozen final evaluation remain different evidence layers;
- the second direct iPhone correction upgrades `mobile-fixed-canvas-overflow` to repeated while staying briefing/device-scoped;
- repeated human review should use a fast non-authoritative prebuilt Preview, while final acceptance remains unchanged.

The future-task proof is deliberately a different robot-RL lab-meeting request. It must retrieve diagnostic closure, training-dynamics boundaries, whole-slide 16:9 phone fit-to-width with no horizontal drag, the canonical review-preview workflow, and the fact that the current visual candidate is still unaccepted. Evaluation proof injects a `PAIR-084-DIAGNOSTIC-CLOSE-LOOP` recurrence into a synthetic PASS receipt; the Preference Judge must reject it, then pass after that recurrence is repaired.

Conversation-to-ledger extraction remains Agent-interpreted because the repository has no direct ChatGPT turn-export API. The repository validates the structured ledger, source-state distinction, retrieval, severity, tier boundaries, and evaluation proof; it does not claim automatic semantic extraction.
