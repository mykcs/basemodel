# Human Preference Learning closeout — OpenEVO briefing PR #619 — 2026-09-11

Authority: `INGESTION-20260911-OPENEVO-BRIEFING-PR619-ACCEPTANCE` in `src/data/humanFeedbackIngestionCloseouts.ts`.

This record extends the existing OpenEVO briefing ingestion DAG rather than copying earlier feedback. It adds eight candidate signals from the PR #619 cold-read/approval window. Most are deliberately classified as task or workflow facts; the new human-preference state change is the owner's explicit `合并` after exact-version review.

That approval makes PR #619 exact head `6fcef6aa…` a concrete **accepted** briefing result and a **Silver** visual reference. It does not create a Golden template because the owner did not say to reuse this exact visual as a future standard. The older PR #614 No-GDR curve visual remains historical `current-candidate` evidence but is no longer active: PR #619 is the later explicitly accepted HPL-rated briefing reference. This supersession is semantic/current-authority succession, not a claim of Git ancestry.

No new Preference Model dimension or Gold Pair is created. The accepted version reinforces existing mechanisms: event-first human wording, local explanation of real technical terms, scientific claim boundaries, research-decision narrative, self-contained method context, and consistent experiment-chart grammar. Reusing those owners avoids synonym families and overfitting one approval into a new global rule.

The source window intentionally does not learn temporary Preview URLs, moving `main` snapshots, CI pending/READY moments, or the named live experiment numbers. The conversation-lessons closeout instruction is also excluded from HPL to avoid double-ingesting repository governance as a visual preference.

Recompute the machine-readable receipt and future-task Preference Brief with:

```bash
HPL_CLOSEOUT_SHOW_BRIEF=1 npm run feedback:ingestion-closeout -- INGESTION-20260911-OPENEVO-BRIEFING-PR619-ACCEPTANCE
```

The evaluation proof deliberately omits the already-hard `internal-detail-promoted-to-primary-attention` check from a synthetic candidate receipt; the verifier must reject it, then pass after the family is restored. This proves the existing repeated lesson still blocks recurrence before owner review while the new acceptance updates trajectory/tier state instead of inventing a new failure family.

Conversation-to-ledger extraction remains Agent-interpreted because the repository has no direct ChatGPT turn-export API. The repository validates the ledger, lineage, exact acceptance state, retrieval, scope, visual-tier boundary, and evaluation proof; it does not claim model-weight learning or automatic chat ingestion.
