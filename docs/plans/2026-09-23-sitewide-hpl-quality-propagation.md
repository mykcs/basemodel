# BaseModel site-wide HPL quality propagation — 2026-09-23

## Goal

Propagate the reader-quality mechanisms proven on the heavily reviewed
`bounded-effective-state-gdr` route across the BaseModel public site without
copying that page's visual template.

The reference route is a **quality witness**, not a universal layout.

## Authority

Apply, in order:

1. current owner instruction;
2. current scientific / factual / deployment authority;
3. BaseModel Wish / Reader Contracts;
4. central website-learning HPL:
   - zero-context reader;
   - Chinese-first / useful English only;
   - concrete actors and actions;
   - result-first research reading;
   - metric = meaning -> result -> interpretation/boundary;
   - evidence stays near the claim;
   - numbers state what they count;
   - internal IDs are provenance, not narrative;
5. current project executable guards.

## Propagation layers

### A. Whole-site human-expression baseline

- [x] Remove remaining high-confidence presenter wording from public copy.
- [x] Replace reader-facing status shorthand such as bare `Pending`.
- [x] Explain project terms such as successor revision at first use.
- [x] Remove ambiguous relative-time wording where a dated or live-state meaning is intended.
- [x] Put explicit hardware quantities in historical/live/allocation/example context.
- [x] Remove stale legacy product identity from English metadata.
- [x] Add regression guards so these families do not return.

### B. Research-result quality contract

Canonical multi-metric result owners:

- [x] `OpenEvoEffectiveStateGdrLoraStudy.astro` — reference witness.
- [x] `OpenEvoQ17DirectApplyAnalysis.astro`.
- [x] `OpenEvo7BStage2AnalysisMap.astro`.
- [x] `OpenEvoExperimentResultsScaffold.astro`.
- [x] `OpenEvoStage1LearningObjectives.astro`.

Required semantic order, skipping unavailable dimensions rather than inventing them:

`direct result -> optimization/training -> parameter/geometry -> behavior/function -> synthesis/limits -> next question`

For metric subsections:

`what/why -> observed result/evidence -> interpretation + what it cannot prove`

### C. Site-role boundaries

Do **not** force the Bounded/GDR paper structure onto:
- operational/run pages;
- model/paper/catalog references;
- decision workspaces;
- archives;
- deep mechanism pages.

Those routes receive the same zero-context / human-language / evidence-boundary
standard, but preserve their own semantic role.

## Acceptance

- [x] audience-copy candidate queue reviewed; all high-confidence public-page hits repaired or explicitly classified.
- [x] new hard guards pass.
- [x] `npm run verify:fast`.
- [x] `npm run build`.
- [x] rendered human-expression + math audits.
- [x] representative 390 / 768 / 1440 browser acceptance.
- [ ] Public PR CI.
- [ ] Fast Review Preview.
- [ ] exact-head Vercel final gate before merge.

## Claim boundary

This work improves explanation, navigation, hierarchy, and evidence placement.
It must not change sealed scientific values, experiment identities, chronology,
unknown states, or causal claims.


## Local acceptance evidence

- `verify:fast` exit 0 on the complete current diff.
- `npm run build` produced 265 static routes; heading, official-brand-link, rendered-math, and rendered-human-expression audits all PASS.
- Targeted HPL / result-contract / audience-copy structural tests: 33 / 33 PASS.
- Updated stale human-expression contracts: 23 / 23 PASS.
- Reference Bounded/GDR + site Reader Contracts: 27 / 27 browser tests PASS, including all 68 public reader-contract patterns at phone and desktop first screens.
- Representative non-research / research UI safety and visual closeout: 17 / 17 PASS across phone/tablet/desktop and light/dark.
- Capability-exploration 390px full-prose readability / overflow check: PASS.
- Scientific values, experiment IDs, sealed endpoints, and final-panel semantics were not changed by this propagation pass.
